2026-07-06
- ex/1.zip과 ex/2.zip을 원본 arc.zip과 바이너리 비교했다.
- ex/1.zip은 COMM.IMG와 S1071.DAT만 변경된 제한 패치임을 확인했다.
- ex/2.zip은 COMM.IMG와 S1011/S1021/S1071/S1072.DAT만 실제 변경됐고, 나머지 DAT는 원본 복구 파일임을 확인했다.
- story_test_01은 ex/2.zip의 폰트와 S1071.DAT만 사용하여 초반 신전 입구 검수용으로 분리한다.
- S1071.DAT 0x4795B의 의심스러운 0x04를 원본 종료값 0x00으로 복원한다.
- 03_output/story_test_01_patch_only.zip 생성 및 바이너리 검증 완료.
- 출력 ZIP SHA256: 520983006EFEB541677740A5D5CC81D4682FAFFDB6DD454D796451B24B590D62
- COMM.IMG는 ex/2.zip과 동일하고 S1071.DAT는 0x4795B 한 바이트만 다름을 확인했다.
- story_test_01 DuckStation 검수 실패: 의미 없는 한글 잡문자와 다른 장면의 일본어 글리프 오염 확인.
- story_test_02는 원본 COMM.IMG/S1071.DAT에서 고정 문자표로 다시 생성했다.
- 네 대사 블록 밖의 DAT 변경이 없고 네 종료값 0x00이 모두 보존됨을 검증했다.
- 24개 글리프 셀과 blank cell 0의 배치 검증을 완료했다.
- story_test_02 출력 SHA256: D4389C5EB507CCAEC58629AFD87F756B0C6ECB024794A223F8E1F721064CB43C
- story_test_02 DuckStation 검수에서 고정 문자표 연결과 네 블록 진행은 성공했다.
- ex/1에서 가져온 글리프는 여러 글자가 오인될 정도로 품질이 낮아 폐기 대상으로 판정했다.
- story_test_03은 DAT를 유지하고 COMM.IMG의 24개 글리프만 굴림 12px 1비트로 재생성했다.
- story_test_03 출력 SHA256: E57798F547897AE8AD7332DD81C3A0A0272C24065E115E6F2A0CE534F7816BD7
- story_test_03 DuckStation 검수: 네 대사와 굴림 1비트 글꼴 성공, 메뉴/UI 저번호 슬롯 충돌 확인.
- story_test_03 성공본을 99_backup/story_test_03_font_success.zip으로 보존했다.
- story_test_04에서 filler/한글을 cell 38-62의 높은 코드 구간으로 이동했다.
- 원본 cell 0-37 보존과 네 DAT 블록 이외 무변경을 검증했다.
- story_test_04 출력 SHA256: A001AE02B71A8785B3DF28D38D0D9FFE48AFE9A4B3A874102FB698169B48A4F6
- story_test_04 DuckStation 검수 실패: E0/E4 글자 누락, FC 출력 뒤 이벤트 중단.
- story_test_05는 cell 25-49 안에서 문자와 filler를 재배치하고 E0-FF를 제외했다.
- 원본 cell 0-24 보존, 네 DAT 블록 외 무변경을 검증했다.
- story_test_05 출력 SHA256: 72FAD387151F6D6F8D788AB25424B9AF4E8546707CC42D2F65C47D08E4DD0073
- story_test_05 DuckStation 검수에서 test04의 진행 중단 해결을 확인했다.
- story_test_05 성공본을 99_backup/story_test_05_progress_success.zip으로 보존했다.
- story_test_06에 촌/장 두 글리프와 첫/세 번째 블록의 촌장 이름줄을 추가했다.
- test05 대비 변경이 폰트 cell 50-51과 두 DAT 블록에만 있음을 검증했다.
- story_test_06 출력 SHA256: EAC88E54C9AD9054AC5D825900C9AD1D9DC97D5C585EBED371A73EC67BB332FC
- story_test_06 DuckStation 검수에서 촌장 이름줄 표시 성공.
- story_test_06 성공본을 99_backup/story_test_06_names_success.zip으로 보존했다.
- story_test_07에서 0x9C를 문장 내부 공백으로 사용해 두 문장의 띄어쓰기를 적용했다.
- COMM.IMG는 test06과 동일하고 DAT 차이는 첫/세 번째 블록뿐임을 검증했다.
- story_test_07 출력 SHA256: 2A09829F936BD6BF6D4EB8D9A614656B3E993ECE93B7F86B71D8637488E63125
- story_test_07 DuckStation 검수 성공. S1071 안정 기준으로 확정했다.
- story_test_07 성공본을 99_backup/story_test_07_stable.zip으로 보존했다.
- 1바이트 안전 슬롯의 남은 용량이 약 3글자라 2바이트 글리프 확장이 필요함을 확인했다.
- story_test_08에서 DD 0B / D4 25의 두 가지 글리프 위치 공식을 동시에 검사하는 locator를 제작했다.
- story_test_08 출력 SHA256: 102974492865E758521D2AFE2815F9234A08B5F106BD9FBA3EB723EEE6766BBC
- story_test_08 DuckStation 결과 원본 村長 유지: map A/B 실패.
- 두 번째 바이트도 하위 2비트를 제외하는 map C 공식을 도출했다.
- story_test_09에서 DD 0B와 D4 25의 map C 위치 두 곳만 촌/장으로 교체했다.
- story_test_09 출력 SHA256: D2644D371B415239C283A90E4778C575A499B903EEA656FE72745CE3883C8877
- story_test_09 DuckStation 결과 원본 村長 유지: map C 실패.
- story_test_10에서 2바이트를 64x64 행/열로 보는 네 보정 조합을 한 번에 검사하도록 구성했다.
- story_test_10 출력 SHA256: 3BCDBC9AB0C4423CEA3917B15ABEE1ED8C144EE8EBC7EE0A0E4F9E7DBC246322
- 2026-07-07: story_test_10 result checked again; title screen/logo corruption confirmed in DuckStation.
- 2026-07-07: The 2-byte locator guess is rejected for now. Keep story_test_07 as the stable baseline.
- story_test_11은 story_test_07 안정본에서 D4/D8/DC 남은 1바이트 슬롯만 검사하도록 구성했다.
- story_test_11 변경: COMM.IMG cell 52-54에 마/을/로 추가, S1071 두 번째 블록만 "마을로 / 가라"로 변경.
- story_test_11 출력 SHA256: 88BCDE15A1803B569BDE3130525DE9F62BD24B03AB1C2F13F40BC85B24BF4A60
- story_test_11 DuckStation 검수 성공: 타이틀/첫 대사/두 번째 대사/세 번째 대사/진행 모두 정상.
- story_test_11 성공본을 99_backup/story_test_11_remaining_slots_success.zip으로 보존했다.
- D4=마, D8=을, DC=로를 현재 안전 1바이트 슬롯으로 확정했다.
- story_test_12는 story_test_11 성공본의 COMM.IMG를 유지하고 S1071.DAT를 story_test_07 안정 대사로 복원했다.
- story_test_12 정적 검증: S1071.DAT는 story_test_07과 완전 동일, COMM.IMG는 마/을/로 글리프 추가분만 다름.
- story_test_12 출력 SHA256: A746D25902C235C107B65E9B7FBAE673F3F73205F0DD95A0EAD8B73775F5B370
- story_test_12 DuckStation 검수 성공: story_test_07과 같은 화면/진행을 유지하고 추가 글리프 마/을/로를 보유한 기준본으로 확정했다.
- story_test_13은 story_test_12 기준으로 S1011 첫 블록 0x478AA-0x478CC만 "아크여 / 마을로 가라"로 변경했다.
- story_test_13 정적 검증: COMM.IMG와 S1071.DAT는 story_test_12와 동일, S1011.DAT 변경은 첫 블록 35바이트뿐이다.
- story_test_13 출력 SHA256: 2518D065D106CDD43FCD3900242EB1DA4D1552BB67D0624E573D938C65CD30EF
- story_test_13 DuckStation 검수 성공: S1011 첫 블록에 "아크여 / 마을로 가라" 정상 표시, 다음 블록은 원본 일본어로 이어짐.
- story_test_13 성공본을 99_backup/story_test_13_s1011_first_block_success.zip으로 보존했다.
- story_test_14는 story_test_13 기준으로 S1011 두 번째 블록 0x47902-0x47918을 "이 뒤로 / 가라"로 추가 변경했다.
- story_test_14 정적 검증: COMM.IMG와 S1071.DAT는 story_test_13과 동일, S1011.DAT는 첫/두 번째 블록만 변경됐다.
- story_test_14 출력 SHA256: D7A1A14BBE2138F0FD335FE2A093A6E42135448CEA2D6317FE38486AE932AB4B
- story_test_14 DuckStation 검수 성공: 두 번째 블록 "이 뒤로 / 가라" 정상 표시 및 진행 확인.
- story_test_14 성공본을 99_backup/story_test_14_s1011_two_blocks_success.zip으로 보존했다.
- story_test_15는 story_test_14 기준으로 S1011 세 번째 블록 0x47954-0x47968을 "조심하라"로 추가 변경했다.
- story_test_15 정적 검증: COMM.IMG와 S1071.DAT는 story_test_14와 동일, S1011.DAT는 첫/두/세 번째 블록만 변경됐다.
- story_test_15 출력 SHA256: B882A51DCB3E73172D0A07860DA2F098C35E156770366B01A93956B6F8B2404E
- story_test_15 DuckStation 검수 성공: 세 번째 블록 "조심하라" 정상 표시 및 진행 확인.
- story_test_15 성공본을 99_backup/story_test_15_s1011_three_blocks_success.zip으로 보존했다.
- story_test_16은 story_test_15 기준으로 S1011 네 번째 블록 0x4799E-0x479C7을 "아크 / 돌아올 때까지 / 기다리겠다"로 추가 변경했다.
- story_test_16 정적 검증: COMM.IMG와 S1071.DAT는 story_test_15와 동일, S1011.DAT는 첫 네 블록만 변경됐다.
- story_test_16 출력 SHA256: 814A872839B7F7D6B16E4537595742C1BA9F585290003526533534B4B11E5994
- story_test_16 DuckStation 검수 성공: 네 번째 블록 "아크 / 돌아올 때까지 / 기다리겠다" 정상 표시 및 진행 확인.
- story_test_16 성공본을 99_backup/story_test_16_s1011_four_blocks_success.zip으로 보존했다.
- story_test_17은 story_test_16 기준으로 S1011의 확인된 텍스트 블록 9개를 한 번에 패치하는 배치 검증판으로 제작했다.
- story_test_17 정적 검증: COMM.IMG와 S1071.DAT는 story_test_16과 동일, S1011.DAT는 9개 텍스트 블록만 변경됐다.
- story_test_17 출력 SHA256: B622294AC659493E4FBE6C88E9360D4E0B713AF5C9F6AE3B093E35F86C723D9A
- story_test_17 DuckStation 검수에서 8번째 블록 앞에 원본 일본어 "よく"가 남아 표시되는 문제 확인. 원인은 블록 시작을 0x47B40으로 잡아 실제 텍스트 시작 0x47B3E의 앞 2바이트를 남긴 것.
- story_test_18은 story_test_17의 8번째 블록 시작점을 0x47B3E로 보정해 다시 제작했다.
- story_test_18 출력 SHA256: 492C1F206F91532EFA7DFC5E9E39A5F4902B745043C979475B9AA3894BCE5204
2026-07-07
- story_test_18 DuckStation 검수 성공: test17의 "よく 촌장" 혼합 문제가 해결됐고 이후 진행도 정상이다.
- 성공본을 99_backup/story_test_18_s1011_nine_blocks_fix_block8_success.zip에 보존했다.
- 이 시점부터 S1011은 1대사 단위 검증을 종료하고, 파일/경계/새 글자/종료값 중심의 묶음 검증 단계로 전환한다.
2026-07-07
- story_test_19 생성: S1013 첫 블록에 낮은 코드 슬롯 0x08-0x18을 사용해 "고마워 / 누구냐" 임시 검증문을 삽입했다.
- 목적은 번역 확정이 아니라 추가 한글 글자 슬롯 확보 검증이다.
- 출력: 03_output/story_test_19_low_slot_probe_s1013_patch_only.zip
- SHA256: C150C8222E95D3DF045A25FE41E7D6EB79043F14C17935FD4FB7D99EE48D4AC9
2026-07-07
- story_test_19 관찰 결과, 실제 다음 화면은 S1013이 아니라 S1072였다. 따라서 test19는 실제 진행 위치 패치로 채택하지 않는다.
- story_test_20 생성: story_test_18 성공본을 베이스로 1/S1072.DAT 첫 실제 화면 2개 블록을 패치했다.
- S1013 변경은 포함하지 않았다.
- 출력: 03_output/story_test_20_s1072_first_blocks_patch_only.zip
- SHA256: 14DF784904927DD5214A0A9C65E1B3D886567474763AE08B998C30076738B025
2026-07-07
- story_test_20 관찰 결과, S1072의 앞쪽 표시 블록 0x478CC/0x47932가 남아 있어 일본어/한글 혼합이 심하게 발생했다.
- 낮은 슬롯 확장 후에는 같은 시퀀스의 미번역 일본어 블록을 남기면 안 된다는 기준을 추가한다.
- story_test_21 생성: story_test_18 성공본을 베이스로 S1072 표시 블록 7개를 묶어서 패치했다.
- 출력: 03_output/story_test_21_s1072_visible_batch_patch_only.zip
- SHA256: 8AF42D0C093E08DE5860F51A1E92A9A694028571488D4330A09B16538DB87EE5
2026-07-07
- story_test_21 관찰 결과, S1072 신전 내려온 뒤 대부분은 변환됐지만 portrait가 붙은 0x47C24 근처 블록 하나가 혼합 출력됐다.
- story_test_22 생성: story_test_21을 베이스로 S1072 0x47C24-0x47C5A 블록을 추가 패치했다.
- 집안/내부 혼합은 별도 파일/별도 시퀀스로 보고 다음 단계에서 분리 처리한다.
- 출력: 03_output/story_test_22_s1072_add_kukuru_block_patch_only.zip
- SHA256: C02E8B6822011720C4F39816223E46FE692B87D502F7B074E409C3453B21BC56
2026-07-07
- story_test_22 재검수에서 portrait 혼합 블록이 여전히 보이고, 다음 대사가 비정상적으로 빠르게 넘어가는 증상이 보고됐다.
- S1072 일부 블록에서 첫 00 00 경계 뒤 제어부까지 건드린 가능성을 확인했다.
- story_test_23 생성: story_test_18 성공본과 원본 S1072를 베이스로, 각 S1072 블록을 첫 00 00 경계 전까지만 덮도록 재작성했다.
- 출력: 03_output/story_test_23_s1072_safe_boundaries_patch_only.zip
- SHA256: 944CD6D2B26E8C812D6679680643495CB438BF4AAFD85A562D3B207443F179DD
2026-07-08
- story_bulk3_plus_s1072_room_patch_only.zip 생성.
- 기준은 story_test_18이 아니라 story_progress_bulk3_all_1이며, 19-25 실패 분기는 배포 기준에서 제외.
- 출력 ZIP에는 게임 교체 파일 11개만 포함하고 문서/리포트 파일은 제외.
- S1072는 story_patch_manifest.csv의 10개 블록을 벌크3 기준으로 재적용.

2026-07-08
- story_bulk3_plus_s1072_room_patch_only.zip 사용자 화면에서 한글/일본어 글리프 혼합 확인. 폐기.
- 낮은 슬롯 글리프를 쓰지 않는 안전판 story_bulk3_s1072_room_highslot_patch_only.zip 생성.
- 해당 안전판은 story_progress_bulk3_all_1_gamefiles_only와 파일 해시가 동일하며, plus 회귀를 되돌리는 기준 파일로 사용.

2026-07-08
- 집 내부 어머니 대화 잔여 일본어가 1/S1013.DAT가 아니라 D/SD012.DAT, D/SD013.DAT 쪽에도 존재함을 확인.
- D/SD012.DAT, D/SD013.DAT의 "母さん" 계열 시작 패턴을 high-slot 문장으로 교체한 story_bulk3_plus_d_house_patch_only.zip 생성.
- 낮은 슬롯 글자는 사용하지 않음. 출력 ZIP에는 게임 파일만 포함.

2026-07-08
- 방안 Ark portrait 잔여 원문 스크린샷을 재분석했다.
- 화면과 가장 강하게 맞는 원문 패턴은 원본 1/S1041.DAT 0x48498 계열이지만, story_bulk3_plus_d_house_patch_only.zip 내부의 1/S1041.DAT에서는 이미 제거되어 있었다.
- 적용/패키징 누락 가능성을 줄이기 위해 동일 내용 재전달 ZIP story_bulk3_plus_d_house_repack_verify_patch_only.zip을 생성했다.
- ZIP SHA256은 기존 D-house ZIP과 같은 05B21B7C09983A7EEB9CE7F17E3525221CB07592633639E0DD9E2FFB9D866458이다.
2026-07-09
- AGENTS.md Git rules updated: Codex may commit/push without asking each time when it judges the checkpoint is useful.
- Safety condition retained: original game data, generated outputs, patch ZIPs, extracted binaries, tools, and .gitignore-excluded files must not be committed.

2026-07-11
- Added `05_docs/ui_analysis_2026-07-11.md` with the first static UI source analysis result.
- Restored the current-user Python 3.12.10 and Pillow 12.3.0 environment; all `02_scripts` compile successfully.
- No game binary, baseline patch, or patch-only ZIP was changed.
- Generated the verified 13-file distribution ZIP `03_output/arc_the_lad_kor_2026-07-11_patch_only.zip` from the latest D-house baseline.
- Built `arc_the_lad_kor_2026-07-11_verified.cue/bin` from that 13-file ZIP and verified the packaged file payloads before emulator testing.
- Reclassified and removed the temporary CUE/BIN build after reviewing the original XML's required disc metadata. Delivery remains patch-only ZIPs; user packaging uses the original XML workflow.
- User rejected the archived 2026-07-08 D-house ZIP as a discarded file. It is no longer a delivery candidate; no replacement patch has been released yet.
- Created `story_s1041_ark_mother_new_patch_only.zip`: a new two-file controlled patch for the uniquely confirmed S1041 Ark portrait dialogue block.
- Created `story_house_sd011_extend_patch_only.zip`: a 14-file house-candidate extension that adds one high-confidence D/SD011 dialogue block while preserving existing patch payloads.
- Created `story_house_dialogue_all_s1041_patch_only.zip`: a 14-file house-dialogue batch replacing all 19 confirmed S1041 portrait-dialogue candidates with high-slot Korean text.
- Confirmed the unchanged screenshot was caused by DuckStation loading a pre-patch automatic resume state; the out files and newly built BIN both contain the intended S1041 patch.
- Retracted the stale-resume diagnosis after the user confirmed a full boot from the new build. Reclassified S1041 as a non-runtime source for this screen and created `story_house_sd011_all_dialogue_patch_only.zip` for all nine D/SD011 dialogue candidates.
2026-07-11
- Extended `analyze_dialog_blocks.py` to recognize high-confidence `17 00 00 00` dialogue blocks while preserving their `00 00` prefix.
- Added `story_house_s1021_control0000_manifest.csv` for the nine live-RAM-confirmed S1021 house dialogue blocks.
- Built patch-only `story_house_s1021_control0000_dialogue_patch_only.zip` from the latest cumulative 14-file patch.
2026-07-11
- Added the ten missed S1041 `control_0000` dialogue blocks to the cumulative house/snow patch.
- Updated `build_story_from_manifest.py` so `BUILD_REPORT.txt` remains work metadata and is excluded from patch-only ZIPs.
- Built `story_house_s1021_s1041_complete_dialogue_patch_only.zip` with 14 replacement files only.
2026-07-11
- Added the ten remaining S1021 `control_0000` house/snow dialogue blocks after confirming the active RAM matched the patched S1021 file.
- Built cumulative patch-only `story_house_s1021_all_control0000_dialogue_patch_only.zip`.
2026-07-11
- Added the four missed S1022 snow-sequence `control_0000` dialogue blocks.
- Made Pillow loading lazy in `build_story_from_manifest.py` so high-slot-only patch builds do not require font-rendering dependencies.
- Built `story_house_s1021_s1022_complete_dialogue_patch_only.zip`.
2026-07-11
- Retracted S1022 as the source of the four still-untranslated snow portrait screens.
- Added a targeted D/SD031 manifest and analysis input for four `03 00`/`04 00` prefix dialogue bodies found in live RAM.
- Built `story_house_d_sd031_snow_dialogue_patch_only.zip`.
2026-07-11
- Retracted D/SD031 as the source of the target portrait screen.
- Patched the live-RAM-confirmed extended-prefix block `D/SD011.DAT:0x47B60`.
- Built `story_house_live_sd011_extended_dialogue_patch_only.zip`.
2026-07-11
- Added live-confirmed `1/S1031.DAT:0x4787A` after finding the same original body in the built BIN and identifying S1031 as its unpatched copy.
- Built `story_house_live_s1031_blocking_dialogue_patch_only.zip`.
2026-07-12
- Decompressed DuckStation save-state slots 2-7 and identified the loaded source files from PS1 RAM fingerprints.
- Added two live-confirmed remaining `D/SD011.DAT` payloads at `0x47A20` and `0x47C5A` to the cumulative patch-only ZIP.
- Built `story_house_sd011_live_slots_2_to_4_patch_only.zip`; only `D/SD011.DAT` differs from the prior cumulative ZIP.
2026-07-12
- Rechecked newly saved DuckStation slots 1-3 against the latest patch-only ZIP.
- Confirmed all delivered D/SD011 and analyzed S1041 payloads are already present in the states; made no speculative binary edit.
2026-07-12
- Added the live slot-1 D/SD011 extended-form payload at `0x47D58` after confirming it remained original in the post-patch save state.
- Built `story_house_sd011_live_slots_1_to_4_patch_only.zip`.
2026-07-12
- Added `02_scripts/duckstation_gdb_watch.py`, a one-connection GDB watchpoint client for future live dialogue-source tracing.
- The script configures a watchpoint, resumes on the same socket, records the stop state, and sends a final resume packet before it disconnects.
2026-07-12
- Extended `duckstation_gdb_watch.py` with accepted-value filtering, multiple-hit tracing, immediate per-hit JSON persistence, and stop-aware final resume behavior.
- No game file or patch-only ZIP was changed during this trace attempt.
2026-07-12
- Extracted DuckStation save-state thumbnails and confirmed the two remaining screens are `誰？` and `なぜ？`.
- Added the omitted 3-byte S1041 payloads at `0x47EA4` and `0x480C0` using stable high-slot Korean plus the existing question-mark byte.
- Built `story_s1041_short_questions_patch_only.zip` from the latest 16-file cumulative baseline.

2026-07-12
- Added seven bounded replacements in `21/S2011.DAT` to `story_s2011_city_dialogue_patch_only.zip`.
- Output has 17 game files, no report file, and SHA256 `8EA1EA6A64D1CD561E77A7C408FA7E59F5816F8EBC9D7AF1B0D6B2C6DC9681AE`.

2026-07-13
- Added `02_scripts/build_story_s2051_throne_mapping.py`.
- Rebuilt the S2051 output as `story_s2051_throne_merged_patch_only.zip` after identifying that the first output had overwritten prior S2051 replacements with original data. The corrected ZIP preserves the prior S2051 payload outside the new ten bounded replacements.
- Rebuilt the user-facing output as the 29-file cumulative `story_s2051_throne_full_patch_only.zip`, combining all current accepted story patch files with the corrected S2051 payload.
- Added the latest-state-backed `22/S2054.DAT` throne-room payload to the 30-file cumulative `story_throne_s2054_full_patch_only.zip`.

2026-07-13
- Added `02_scripts/map_savestate_script.py` to map DuckStation save states to the DAT script block actually loaded in PS1 RAM.
- Verified the current event-return sequence as `22/S2053.DAT -> F/SF091.DAT -> F/SF0B1.DAT` with exact 1792-byte window matches.
- Retracted the earlier S2054 source selection for the post-event target; no game data or patch archive was changed in this analysis step.

2026-07-13
- Added `05_docs/story_sf0b1_return_translation.csv` with 17 translated post-event throne-room dialogue bodies.
- Added `05_docs/korean_charmap_extended.csv` with 94 Korean glyphs assigned to unused `E0 xx` story codes.
- Added `02_scripts/build_story_sf0b1_return_full.py`, which patches `F/SF0B1.DAT`, writes isolated font bitplanes in `COMM.IMG`, and preserves the accepted cumulative baseline.
- Built the 30-file cumulative `story_sf0b1_return_full_patch_only.zip` from `story_s2051_throne_full_patch_only.zip`.

2026-07-13
- Recorded the first SF0B1 runtime result: correct scene and working extended Korean glyphs, with Japanese remnants caused by legacy charmap entries.
- Reassigned the thirty legacy characters used by this scene to verified unused extended codes and added eight characters required by the shortened final prose.
- Shortened bounded translations without changing the scene's story meaning so every all-extended payload fits its original byte capacity.
- Built `story_sf0b1_return_full_korean_patch_only.zip` with 132 verified extended glyphs and no legacy text codes in the 17 translated bodies.

2026-07-13
- Analyzed four enemy-turn DuckStation states and located serialized PS1 VRAM at state offset `0x202000`.
- Confirmed `COMM.IMG` rows appear in VRAM at 2048-byte stride and that the cursor animation samples the extended-font region around `UV=(32,160)`.
- Reserved font columns 9 and 10, relocated fourteen extended characters, and added a build-time rejection for future cursor-column assignments.
- Built `story_sf0b1_return_cursor_safe_full_patch_only.zip`; the reserved cursor columns now match the accepted base COMM data exactly.

2026-07-13
- Retracted the incorrect cursor column-9/10 mapping after the first cursor-safe build failed runtime verification.
- Reconstructed four fresh save-state framebuffers at native 320x240 resolution and aligned their 32x32 cursor sprites.
- Matched the cursor index pattern to COMM rectangle `x=0..31, y=128..159`, relocated fifteen conflicting glyphs, and changed the build guard to reserve rows 11-13 / columns 0-2.
- Built `story_sf0b1_return_cursor_fixed_full_patch_only.zip` and verified the entire cursor source rectangle against the accepted COMM baseline.

2026-07-13
- Added `02_scripts/extract_story_corpus.py` and `05_docs/japanese_charmap_manual.csv` for repeatable high-confidence story-corpus extraction from the original DAT/COMM data.
- Added `05_docs/story_s4041_bulk_translation.csv` with five natural Korean translations for `4/S4041.DAT`.
- Added sixteen unused, cursor-safe codes to `05_docs/korean_charmap_extended.csv` and added `02_scripts/build_story_s4041_bulk.py`.
- Built the 31-file cumulative `story_bulk_s4041_cursor_fixed_full_patch_only.zip` from the accepted cursor-fixed baseline.

2026-07-13
- Mapped the current post-throne DuckStation resume state exactly to `31/S3031.DAT` and corrected the earlier user-facing scene-location assumption for S4041.
- Added `05_docs/story_s3031_bulk_translation.csv` with all 27 high-confidence S3031 dialogue bodies and `02_scripts/build_story_s3031_bulk.py`.
- Expanded the verified table to 161 characters. Three mappings were reassigned only after proving their codes occur zero times in the complete 31-file cumulative DAT set.
- Built `story_bulk_s3031_s4041_cursor_fixed_full_patch_only.zip` with 32 cumulative game files.

2026-07-13
- Mapped fresh slots 1-7 to `21/S2041.DAT` at 100% and identified the complete airship crew dialogue/choice sequence.
- Added `05_docs/story_s2041_bulk_translation.csv` with 21 dialogue and menu blocks, including both three-way choices and branch responses.
- Added 32 parsed-corpus-unused E0 glyph mappings and `02_scripts/build_story_s2041_bulk.py`.
- Built the 33-file cumulative `story_bulk_s2041_s3031_s4041_cursor_fixed_full_patch_only.zip`.

2026-07-14
- Mapped the latest slot 1, slots 2-3, and resume states to `31/S3011.DAT`, `31/S3022.DAT`, and `C1/SC011.DAT` with exact 1792-byte matches.
- Added `05_docs/story_s3011_s3022_sc011_bulk_translation.csv` with 45 confirmed bounded dialogue/menu replacements.
- Added `02_scripts/build_story_s3011_s3022_sc011_bulk.py` and `02_scripts/verify_story_s3011_s3022_sc011_bulk.py` for deterministic cumulative construction and independent reconstruction checks.
- Added 68 parsed-dialogue-unused, cursor-safe glyph mappings, raising `korean_charmap_extended.csv` to 261 entries.
- Built the 36-file cumulative `story_bulk_s3011_s3022_sc011_s2041_s3031_s4041_cursor_fixed_full_patch_only.zip`.

2026-07-14
- Extended corpus decoding for confirmed DD-E0 mappings and speaker selectors `02/05/07 00`; exact high-confidence occurrence coverage reached 97.64%.
- Added `story_s3023_s3024_bulk_translation.csv`, `build_story_s3023_s3024_bulk.py`, and 57 cursor-safe glyph mappings.
- Built the 38-file cumulative `story_bulk_s3011_s3022_s3023_s3024_sc011_s2041_s3031_s4041_cursor_fixed_full_patch_only.zip`.

2026-07-14
- Added `story_intro_s1071_s1011_stable_translation.csv` and `build_story_intro_stable.py`.
- Replaced 13 confirmed intro structural-probe bodies in S1071/S1011 with natural context-aware Korean.
- Added 20 cursor-safe glyph mappings and built `story_intro_stable_v01_cumulative_patch_only.zip`.

2026-07-14
- Rejected intro stable v0.1 after runtime exposed six legacy Hangul mappings.
- Added seven verified extended Hangul mappings and `build_story_intro_e2ff_longtext_proof.py`.
- Built a 39-file proof that replaces S1011 `0x479FC` with `E2 FF` and stores its 71-byte Korean text in a guarded PSX.EXE tail cave.

2026-07-14
- Runtime rejected the E2 FF tail-cave proof: the hook survived, but `0x801A86F0-0x801A871F` was zeroed before the target command executed.
- Added `build_story_intro_e2_pointer_proof_v2.py`.
- Built a replacement proof that restores the original lookup function, stores the long text at S1011 `0x47700`, and redirects temporary E2 name index 107 to RAM `0x80116700`.
- Output SHA256: `C45EB983E310541AD148076E359166CE02729D65EB8F53E0DA6D24B27845FF2C` (deterministic ZIP metadata).

2026-07-14
- Runtime confirmed v2 could display the 71-byte secondary string and auto-wrap it to three lines. Embedded `E6 01` bytes appeared as visible garbage rather than line breaks.
- Added `build_story_intro_e2_bank_proof_v3.py` with sixteen dedicated 128-byte E2 slots and no borrowed name pointers.
- v3 hooks only the text interpreter call at `0x8016BC84`; all direct character/monster lookup calls retain the original path.
- Built `story_intro_longtext_e2_bank_proof_v3_patch_only.zip`, SHA256 `FA84E7A9169C481BFBBD5B18F5285EA132598E2137B84F261935EB1B5AC26416`.

2026-07-14
- Recorded the successful v3 runtime result and promoted the dedicated E2 scene bank from proof to the intro expansion base.
- Added `story_reference_naver_1_21.csv`, indexing all 21 external story-guide posts for scene and speech-tone reference.
- Added `story_intro_e2_expanded_translation.csv` and `build_story_intro_e2_expanded.py` for thirteen context-preserving intro translations using per-scene 128-byte slots.
- Kept short speaker names on the first dialogue line and removed secondary-string line controls so the renderer can auto-wrap across the full window.
- Corrected new-glyph cursor exclusion to use exact rectangle overlap with `x=0..31, y=128..159`, including the lower four pixels of glyph row 10.
- Built `story_intro_e2_expanded_v01_cumulative_patch_only.zip`, SHA256 `A45B8891D01DE74A2D978252E8A5FC3461E36C981CBBE8F859842A9214B43DE9`.

2026-07-14
- Rejected intro E2 expanded v0.1 after runtime showed repeated blank dialogue windows.
- Corrected `build_story_intro_e2_expanded.py` to terminate each inline body immediately after its E2 command instead of filling the remainder with visible spaces.
- Built `story_intro_e2_expanded_v02_cumulative_patch_only.zip`, SHA256 `FFED77C7F4879276D6B050679F931201BE8293E1C173A3E508B62DA47D9A375E`.

2026-07-14
- Rejected intro E2 expanded v0.2 after its early inline terminator skipped the rest of the mountaintop scene and returned to the world map.
- Extended the dedicated E2 handler to read a per-slot inline-skip byte and advance `s0+0x14` to each dialogue's original end before rendering the external string.
- Preserved all original body tails and `00 00` boundaries while bypassing their visible-space padding.
- Built `story_intro_e2_expanded_v03_cumulative_patch_only.zip`, SHA256 `437BBF577E6B37F34E57D4BD9AD385DD45098D105969CE67C833158CDF7CB0FE`.

2026-07-14
- Rejected intro E2 expanded v0.3 after all thirteen expanded strings rendered as empty windows while event progression remained correct.
- Moved per-slot skip metadata from byte 0 to byte `0x7F` and restored visible strings to the aligned slot base proven by v3 runtime.
- Updated the custom E2 handler to read metadata at `slot_base + 0x7F` without offsetting the returned text pointer.
- Built `story_intro_e2_expanded_v04_cumulative_patch_only.zip`, SHA256 `57EE2A984AA1985A3A6C734A0792CB4A0C1427DE5302B606177F0A890C9FC1EC`.

2026-07-14
- Rejected intro E2 expanded v0.4 after runtime again showed empty expanded dialogues.
- Confirmed from EXE control flow that changing `s0+0x14` during E2 lookup terminates the text state before secondary rendering begins.
- Restored the runtime-proven v3 lookup handler and patched E2's completed-secondary path at `0x8016BDC0` to apply inline skipping only after the external string terminates.
- Built `story_intro_e2_expanded_v05_cumulative_patch_only.zip`, SHA256 `2BEEBDC203F92AD882DE1A9511F343FE7494E2EBAAD83BBADE9171E8B810B49F`.
- Corrected the expanded E2 lookup and completion handlers to add the missing RAM-bank displacement `0x4000`; both now address scene slots at `0x80114000 + slot * 0x80`.
- Built `story_intro_e2_expanded_v06_cumulative_patch_only.zip`, SHA256 `D849F637D7F1C0E5B6E170BBE3CB6ACB47E48FC045C9E0A702B60CAF26991FF5`.
- Accepted v0.6 after runtime showed expanded S1071/S1011 text, five-row automatic wrapping, and normal scene continuation.
- Rejected the v0.7 per-character wrapper after runtime failed to enter normal emulation and remained at game 0 FPS.
- Added `build_story_intro_dialogue_width11_v08.py`, which uses only existing dialogue-state reset delay slots and introduces no new executable wrapper.
- Built the cumulative 11-pixel dialogue-width isolation trial `story_intro_dialogue_width11_v08_cumulative_patch_only.zip`, SHA256 `04A6162400C02F1BEC2397A28F284552EC1EA836BF135278976BDBDBCD359763`.
- Rejected v0.8 after runtime showed only a subtle spacing change and clipped the right edge of wide Hangul glyphs.
- Added `build_story_intro_halfwidth_space_v09.py`; it retains the accepted v0.6 font and 12-pixel glyph width while reducing only space index `0x9B` to a 6-pixel X advance.
- Built `story_intro_halfwidth_space_v09_cumulative_patch_only.zip`, SHA256 `508FCD26D7D758FFF12CF3B63001A799E4D0AEDB507EBE5F335AB20CEE2A1F10`.

2026-07-15
- Freshly launched the same v0.9 CUE without a GDB client; the opening and static title rendered, but this was not a sufficient runtime acceptance test.
- Rejected v0.9 after pressing Start caused blue title-menu rectangles and game 0 FPS in the fresh no-GDB process.
- Identified and corrected an R3000 load-delay violation in the non-space X-advance branch.
- Added `build_story_intro_halfwidth_space_v10.py` and built `story_intro_halfwidth_space_v10_cumulative_patch_only.zip`, SHA256 `D11C4C629C4718414D45C8AC000EF61965EC9B4309BC24AD74BD995CCE247335`.
- Reclassified the v0.7 wrapper 0 FPS result as inconclusive because it was measured in the same GDB-affected process; no wrapper code was restored.
- Found three R3000 load-delay violations in the E2 deferred inline-skip helper; these prevented slot skip metadata from being applied and exposed original tails such as `속 지킬 거야?`.
- Added `build_story_intro_e2_skip_v11.py` and built `story_intro_e2_skip_v11_cumulative_patch_only.zip`, SHA256 `84490A7196300C903A8B1E21A408556A10D79E0406534B921557B6D75D6A88AB`.
- Accepted v0.11 after runtime confirmed the residual inline tail disappeared and dialogue progression remained correct; promoted it as the cumulative base for bulk patching.

2026-07-15
- Added `build_story_punctuation_v12.py` to correct the vertical placement of comma `DFE2` and period `E060` without changing their character codes.
- Updated the shared glyph renderer so future font rebuilds retain the same punctuation baseline.
- Built `story_punctuation_v12_cumulative_patch_only.zip`, SHA256 `4CE0465201FE204A542413271C2FDC1C52E7F7DC793224F9D533B855A2D4CDFB`.

2026-07-15
- Added `story_s1072_e2_translation.csv` with contextual translations for all ten high-confidence S1072 dialogue bodies.
- Added `build_story_s1072_e2_v13.py`; it assigns E2 slots `0x81-0x8A`, preserves inline return offsets, allocates 13 safe glyphs, and checks cursor preservation.
- Removed the old temporary S1072 lines such as `촌장 / 가라` from the cumulative output.
- Built `story_s1072_e2_v13_cumulative_patch_only.zip`, SHA256 `4E413AB2874F4773F431EDA592625241BC4BEE50280D78EFD563B334A426F8AE`.

2026-07-16
- Added `build_story_e2_bank79_v14.py`, expanding the per-scene E2 external bank from 16 to 79 slots while reserving original E2 ID `A9`.
- Repaired the missing low-slot planes for `괄`, `덕`, and `량` exposed by the S1072 runtime screenshot.
- Added `story_house_to_throne_e2_translation.csv` with all 185 high-confidence dialogue bodies across 14 house-to-throne scene files.
- Added `build_story_house_to_throne_e2_v15.py`; it selects only empty per-file slots, allocates parsed-dialogue-safe glyphs, migrates colliding glyph indices, and verifies every external payload.
- Built `story_house_to_throne_e2_v15_cumulative_patch_only.zip`, SHA256 `196AB88F3AAF7956D5194723498D791267662D405075880121276E4ABDAAAD68`.

2026-07-16
- Corrected the v0.15 scope error exposed by runtime screenshots: early bulk placeholders remained in `S1013/S1021/S1022/S1023` even though the later 14-file batch was translated.
- Added `story_intro_missing_e2_translation.csv` with all 103 high-confidence corpus bodies in the four omitted files.
- Added `build_story_intro_missing_e2_v16.py`; 100 normal bodies use E2, while three S1023 choice bodies preserve original `E5 03` and `E6 01` controls inline.
- Added 45 safe glyphs and built `story_intro_complete_e2_v16_cumulative_patch_only.zip`, SHA256 `61D58B4640BC03C581AEF9785EA95B87BA183568835FB2D4F62A695F3A23F762`.

2026-07-16
- Added `story_verified_returns_e2_translation.csv` with 35 verified short, aftermath, cave-event, and post-battle entries.
- Added `build_story_verified_returns_e2_v17.py`; it supports partial-file exception manifests, restores retracted speculative files, and rejects active historical auto-placeholder bodies across the cumulative DAT set.
- Replaced 34 missed bodies through E2, restored the one-byte S2061 exclamation, and preserved the accepted `S2053/SF0B1` event-return files byte-for-byte.
- Added two safe glyphs and built `story_verified_returns_e2_v17_cumulative_patch_only.zip`, SHA256 `0CC3244DDE10CD8267248E984151DFA89BA788FCDE7C7A39C38B82E9A5E5CCDD`.

2026-07-16
- Mapped the latest slot 1 exactly to `31/S3012.DAT` and confirmed that its fourteen high-confidence Choppin/help bodies were absent from the cumulative patch.
- Added `story_legacy_tone_e2_v18_translation.csv` with 53 entries covering naturalized `SF0B1`, complete `S2041`, and complete `S3012` dialogue.
- Added `build_story_legacy_tone_e2_v18.py`; it supports ordinary E2 strings, control-preserving inline choices, and a hybrid E2 prompt that resumes at the original choice marker.
- Added seven safe glyphs and built `story_legacy_tone_e2_v18_cumulative_patch_only.zip`, SHA256 `3263B9269EA7582E7F4C165D46B7EFB63FD112808250686BAAFCD8DBF1C06588`.

2026-07-16
- Corrected the character-label policy after source review: `大臣` remains `대신:` while personal-name references `アンデル` remain `안델`.
- Added `story_minister_s3022_s3031_e2_v19_translation.csv` with eighteen corrected minister labels, two omitted S2012 lines, 35 rebuilt S3022 E2 lines, and 27 rebuilt S3031 E2 lines.
- Added `build_story_minister_s3022_s3031_e2_v19.py`; it restores S3022/S3031 from clean originals before E2 insertion, preserves the S3022 choice bytes, and rejects any out-of-range control/portrait changes.
- Added ten safe glyphs and built `story_minister_s3022_s3031_e2_v19_cumulative_patch_only.zip`, SHA256 `5558DAE42D6EBD0CED2DA238512F30EE451007E731F7E9B4A5CC5E591A77A171`.

2026-07-16
- Added `audit_all_story_files_v20.py` and deterministic CSV reports covering every extracted game file and every high-confidence story body.
- Corrected the earlier incomplete-coverage estimate: 2,103 high-confidence bodies in 134 files remain identical to their Japanese originals after v0.19.
- Added `story_choice_layout_v20_translation.csv` and `build_story_choice_layout_v20.py`.
- Reflowed the two S1023 help menus into one-column choices and shortened the S3012 Choppin prompt without modifying its option bytes.
- Built `story_choice_layout_v20_cumulative_patch_only.zip`, SHA256 `213717051809418251E5765D3BC72983990ADEE7A147F004FE4E7F2276C14AF4`.

2026-07-16
- Added `audit_choice_bodies_v21.py`, identifying all 265 high-confidence E5 choice bodies across 42 files.
- Added `plan_all_choice_translations_v21.py` and the complete `story_all_choices_v21_translation.csv` manifest.
- Added `build_story_all_choices_v21.py`; it preserves eight verified choices, rewrites 257 choices, restores eight incorrect E5 marker types, and validates every original boundary and marker sequence.
- Added 35 previously absent choice-bearing files to the cumulative replacement set without modifying bytes outside verified choice ranges.
- Built `story_all_choices_v21_cumulative_patch_only.zip`, SHA256 `A050991D8D4400081689A6F72F730E2221AE328DF74D7D971B9CC5EAFA1D4C4E`.

2026-07-17
- Added `story_v22_dialogue_translation.csv` for the live-identified S2013 and S3011 ordinary dialogues.
- Added `story_v22_fixed_choice_overrides.csv` for fifteen compact option labels in the two files without verified free E2 slots.
- Added `build_story_dialogue_choice_structure_v22.py`; it audits all 358 E5 candidates, excludes 93 binary false positives, and restores original E5/E6 positions in 243 of 265 real choice bodies while retaining Korean text.
- Added deterministic choice audit/report outputs. Independent byte-range verification found zero changes outside declared choice bodies, allocated E2 slots, and the two dialogue commands.
- Built `story_dialogue_choice_structure_v22_cumulative_patch_only.zip`, SHA256 `C5005057BF77F51912A93E5FB4C4EA3F1368BD2CDA6C5A02BF32E55F531254C4`.

2026-07-17
- Runtime evidence showed that v0.22 external choice prompts skipped E6 row controls: option text appeared above the triangular cursor's blank selectable rows.
- Added `story_v23_choice_prompt_lines.csv` with fifteen reviewed two-row prompts, including both Choppin copies, departure, spirit-gate, and tournament confirmations.
- Added `build_story_choice_row_alignment_v23.py`; it repairs all 133 affected external prompts by ending each E2 skip immediately before the original E6 and uses one E2 slot per non-empty source row.
- Verified 133/133 prompt structures, 148 E2 slots, byte-identical option regions, source-exact E5/E6 geometry, deterministic rebuilds, and zero changes outside declared prompt/slot ranges.
- Built `story_choice_row_alignment_v23_cumulative_patch_only.zip`, SHA256 `E0A6C6EF167CFDDDA375FCE2B336A2CEAE478499DDDEB371668E2B99672C3C89`.

2026-07-17
- Added `audit_psx_ui_tables.py` and extracted 503 confirmed `PSX.EXE` pointer-table entries covering equipment, consumables, skills, character/monster names, regions, and locations.
- Added `ui_item_probe_v24.csv` and `build_ui_item_probe_v24.py` for a two-field consumable-item probe based on the unchanged story v0.23 baseline.
- Built runtime-pending `ui_item_herb_probe_v24_cumulative_patch_only.zip`, changing only the `PSX.EXE` name and description slots for consumable index 7. SHA256: `E5755DB4B0C911D406F1D03F752C5241A4681176D7C8FBBC759AA9CE08E0B3E8`.
- Runtime accepted v0.24: the battle item palette displays `약초`, and L1 help displays `체력 20 회복` with correct font, spacing, and digits.

2026-07-17
- Added `ui_consumables_v25.csv` with Korean names and L1 help text for all 29 active consumables while preserving the three original reserved records.
- Added `build_ui_consumables_v25.py`; it repacks the complete consumable name and description blocks at four-byte boundaries and rewrites all 64 matching pointers without using external executable space.
- Built runtime-pending `ui_consumables_v25_cumulative_patch_only.zip`, changing only the four declared `PSX.EXE` string/pointer ranges. SHA256: `2808FD4A0CA191F01BB368AC3915DA27FD5CCA37B801308D3EEA5ACDBC3164C1`.

2026-07-17
- Added `extract_singlefile_guide_images.py` for reproducible extraction and indexing of embedded scan images from locally saved SingleFile walkthrough pages.
- Marked `05_docs/공략/` as local-only third-party reference material; no saved HTML or extracted scan image is tracked by Git.

2026-07-17
- Added `ui_full_v26_data.py` and generated `ui_full_v26.csv`, covering all 503 records from the nine confirmed executable UI tables with project-reviewed Korean terminology and the local walkthrough as a wording reference.
- Added `build_ui_full_v26.py`; it allocates 389 UI-only glyphs in physical indices `1240-1629`, repacks all nine string regions as one deduplicated pool, and rewrites all 503 pointers.
- Added `audit_ui_full_v26.py` for independent pointer, manifest, executable-range, font-plane, cursor, and unrelated-member verification.
- Built `ui_full_v26_cumulative_patch_only.zip`, SHA256 `F31C5BABEE7011A2E9A8B168A971C80E5D8C376F655436ED1E6EBCB67E028549`.

2026-07-17
- Rejected v0.26 after runtime showed character-sprite corruption, an empty accessory list, severe battle-entry delay, and a battle-start lock.
- Added `build_ui_safe_v27.py` and `ui_safe_v27.csv`; the hotfix uses only existing DD-E0 glyph mappings, translates 305 safe records, and preserves v0.25 payloads for 198 records requiring unavailable UI-safe glyphs.
- Built `ui_safe_v27_cumulative_patch_only.zip`, SHA256 `443731B3EA5281121F0993A323B5D340C4777BECD888337A398E12DB25F500D3`.

2026-07-17
- Added `ui_safe_v28_overrides.py` with 237 safe-glyph wording overrides and guide-reviewed player skill names/descriptions.
- Added `build_ui_safe_v28.py`, `audit_ui_safe_v28.py`, `ui_safe_v28.csv`, and `ui_skill_guide_reference_v28.csv`.
- Increased Korean UI coverage to 486/503 records while keeping `COMM.IMG` byte-identical to v0.25 and changing only `PSX.EXE`.
- Built runtime-pending `ui_safe_v28_cumulative_patch_only.zip`, SHA256 `0FCDF709F6125AF083167F67EA998C4BA7B2FD5D774EE4417677DFE934825828`.

2026-07-17
- Added `ui_safe_v29_overrides.py` with reviewed accessory effects and one-line skill-help wording, including `던지기 +1`, `점프 +1`, and `받기 +1`.
- Added `build_ui_safe_v29.py`; it retains the safe v0.28 executable-table model and changes only the legacy `0x6C` physical bitplane to render the skill level prefix as `LV`.
- Added `audit_ui_safe_v29.py` for independent pointer, override, ZIP, help-length, and font-bitplane verification.
- Built runtime-pending `ui_safe_v29_cumulative_patch_only.zip`, SHA256 `4575B705E966001F096E05B5C3F97D7BE49A05D7BAB14ACE67ED26EC6C917F2A`.

2026-07-17
- Added `ui_safe_v30_overrides.py` with deliberate thirteen-cell help wrapping and three `LV +1` equipment descriptions.
- Added `build_ui_safe_v30.py`; it translates four preserved tutorial rows in each of six duplicated scene files through verified empty E2 slots and retains all eighteen original E6 controls.
- Added `audit_ui_safe_v30.py` for independent executable-pointer, help-layout, duplicate-tutorial, byte-range, and cumulative-member verification.
- Built runtime-pending `ui_safe_v30_cumulative_patch_only.zip`, SHA256 `3361FD182D86C5B2B8719DD468F996B23874BC8265ABBC0819B4E2B1F11D8C92`.

2026-07-17
- Runtime rejected the v0.30 padded skill-help layout because the inserted spaces rendered as a large visible gap and did not produce a natural balanced line.
- Added `ui_safe_v31_overrides.py` and restored twelve affected skill descriptions to natural single-line wording of at most thirteen displayed cells.
- Added `build_ui_safe_v31.py`; it relocates 21 save/load and shared system strings through confirmed `PSX.EXE` pointers and repairs 63 battle-confirmation choices in nine files with shared E2 option slots.
- Added `audit_ui_safe_v31.py` plus system and battle-choice manifests. Independent checks preserve all source E5/E6 positions, remove the broken bottom-help E7 icon controls, and reject changes outside declared ranges.
- Built `ui_safe_v31_cumulative_patch_only.zip`, SHA256 `C693D30862AE9681EB2B4F492BBECA3BFE17FF396EF88B132A4035FD7F7384F5`.

2026-07-17
- Corrected the v0.31 field-help interpretation: restored the original `E7 02/E7 03` decision/status button icons and replaced the incorrect back-function wording with status-check wording.
- Added the missed target-selection help pointer at `0x82360` and translated it as `다음 대상을 선택합니다`.
- Rebuilt the shared one-cell `LV` bitmap with a pointed V, fixing both equipment descriptions and the battle skill-level prefix.
- Added `build_ui_safe_v32.py`, `audit_ui_safe_v32.py`, and v0.32 manifests/audit reports.
- Built `ui_safe_v32_cumulative_patch_only.zip`, SHA256 `5046186DB1EE07C576D8F868D4D8F921A8F94762E87739ACF55308048AD0DE37`.

2026-07-17
- Added `build_ui_safe_v33.py`; translated all thirteen battle-help pointers with plain Korean text, widened the shared yes/no spacing, corrected the overflowing-fruit effect to `LV 1 상승`, and refined the single-plane LV bitmap.
- Found a separate seven-entry world-name table and translated six safe-bank names. Preserved only `アリバーシャ` because the required `샤` glyph is unavailable.
- Added `audit_ui_safe_v33.py` and `audit_nonstory_japanese_v33.py` with 503-record UI readback, system/help/world pointer checks, all-file classification, and a separate confirmed-residual report.
- Confirmed eighteen Japanese residual entries, all proper-name copies requiring unavailable glyphs; broad direct decoder hits are recorded as unverified binary candidates rather than text failures.
- Built `ui_safe_v33_cumulative_patch_only.zip`, SHA256 `76C0F4E8B50CA03ACC9B2A50AD5359050DE98DC29D0D3DB25F464ED96F54DFAF`.

2026-07-17
- Added `build_ui_safe_v34.py` and `audit_ui_safe_v34.py` for the reported confirmation-spacing, bottom-help, and compact-LV presentation defects.
- Increased the shared `예/아니요` gap from four to six spaces and changed the iconless bottom help from `결정, 돌아가기` to `결정 : 돌아가기`.
- Rebuilt the shared low-code `0x6C` bitmap as a heavier 30-pixel LV while preserving its neighboring three bitplanes.
- Confirmed that restoring `E7 02/E7 03` in place would overwrite active Korean glyph planes; kept the safe text fallback pending a separately verified icon-texture relocation.
- Built `ui_safe_v34_cumulative_patch_only.zip`, SHA256 `1B49FAA19018A3C06238F4371BCB3A81A4AF9B672837368EA6A6CA492ACBD622`.

2026-07-18
- Added `build_ui_safe_v36.py`; retained the v0.35 non-story coverage, restored the runtime-accepted v0.34 compact LV font byte-for-byte, and changed the acquisition suffix to formal Korean without the broken quote glyph.
- Built `ui_safe_v36_cumulative_patch_only.zip`, SHA256 `B88FCA6C706E59F779197F23B4DCE45C1C0C87AF1274E9B4B316B3D4751276D3`.
- Added `build_ui_safe_v37.py` as a hash-locked two-byte geometry patch over v0.36. It widens shared confirmation boxes by 24 pixels and preserves the absolute positions of all prompt, option, and cursor content.
- Copied the unchanged v0.36 manifests to v0.37 names for traceability and wrote `01_work/analysis/ui_safe_v37/build_report.txt`.
- Built `ui_safe_v37_cumulative_patch_only.zip`, SHA256 `0583FEED2266C883B413260114F331D73FAC12AE1C9A17EB123D559D9EB29AA1`.

2026-07-18
- Added `build_ui_safe_v38.py` and `audit_ui_safe_v38.py` as direct hash-locked v0.37 repair tools.
- Restored the user-requested v0.35 thin pointed `LV` bitmap while preserving all neighboring physical font planes.
- Relocated verified duplicate `E7 02/E7 03` button images into two same-page, charmap-unassigned 12x12 cells and redirected only their U table entries.
- Restored icon-bearing field help as `{결정버튼}결정 {취소버튼}돌아가기` without restoring the damaged original icon rectangles.
- Built `ui_safe_v38_cumulative_patch_only.zip`, SHA256 `D66E6F4F780E3096B604C7699E3E7EB00392EE7C0C4FA457AEDE3F61EEFD45D9`.

2026-07-18
- Added `build_ui_safe_v39.py` and `audit_ui_safe_v39.py` for the two remaining v0.38 runtime defects.
- Relocated only E7_02 from U=0 to same-page U=0x18 and restored the abandoned U=0 rectangle; preserved E7_03 X exactly.
- Repaired the five battle-HUD label pointers at `0x823AC-0x823BC` by moving their payloads to zero pool `0x820A8-0x820BB`.
- Built `ui_safe_v39_cumulative_patch_only.zip`, SHA256 `0778FE435820409F190579D179F8B36FFFCEB02B5F2004FC1E3ACE58741D5DC3`.
- Independent audit confines all changes to two icon rectangles, one icon U byte, five pointer words, and the new 20-byte HUD pool.

2026-07-18
- Froze `ui_safe_v39_cumulative_patch_only.zip` as the fallback baseline for independent UI/battle glyph-storage expansion.
- Added `99_backup/baselines/ui_safe_v39_fallback_2026-07-18/` with the hash-locked artifact, v0.39 builders, manifests, audit reports, and `BASELINE_MANIFEST.txt`.
- Recorded three visual defects that remain in the fallback baseline: battle-HUD `LV`, E7_02 circle/confirm icon, and the battle-cursor dark mark.
- Recorded the untranslated high-confidence body at `31/S3023.DAT 0x47E4C`; the DAT member is present in v0.39, but this body remained original.

2026-07-18
- Added `analyze_psyq_obj_symbols.py` as a read-only Psy-Q object-analysis helper used during loader investigation.
- Added `build_ui_glyph_store_v40.py` and `audit_ui_glyph_store_v40.py` for a sparse independent UI glyph-storage probe based on the frozen v0.39 artifact.
- Compared `COMM.IMG` with VRAM from 55 DuckStation savestates and selected 159 cells that remained zero in every sample. Allocated 278 current Hangul UI glyphs through 384 E1-E8-addressable sparse planes, leaving 106 planes free.
- Preserved all pointer tables and string lengths and limited changed ZIP members to `COMM.IMG` and `PSX.EXE`. The v0.39 LV plane, icon regions, cursor region, HUD payloads, Japanese-preserved rows, story files, and unrelated members remain unchanged.
- Added `ui_glyph_store_v40_probe.csv`, `ui_glyph_store_v40_map.csv`, build report, and independent readback report.
- Built `ui_glyph_store_v40_sparse_probe_patch_only.zip`, SHA256 `C6D50D2252C52B26499F4C1BC26544E14DE99881ACA31E76FC3EC2BA5A6A1B43`. Runtime verification is pending; v0.39 remains the rollback baseline.

2026-07-18
- Rejected v0.40 after runtime checks in DuckStation slots 1-4 showed corrupted or blank UI text and a progression stop.
- Confirmed that the v0.40 sparse COMM cells are loaded correctly and remain byte-identical between the patch and runtime VRAM. The defect is not a glyph-storage collision.
- Disassembled the dynamic UI parser and confirmed that it treats `E1-E8` as commands before glyph decoding. Recorded the v0.41 direction: unused `E9/EA` prefixes plus parser hooks and a sparse physical-index lookup table.
- Kept the frozen v0.39 artifact unchanged as the only accepted rollback baseline.

2026-07-18 v0.41 E9/EA sparse UI glyph runtime candidate:
- Added `build_ui_glyph_store_v41.py` and `audit_ui_glyph_store_v41.py`.
- Rebuilt from the frozen v0.39 ZIP, not from rejected v0.40.
- Reused the 278 runtime-sampled sparse physical glyph planes while replacing the colliding E1/E2 virtual codes with E9/EA.
- Added two dynamic-parser classifier hooks, one common-decoder hook, and a 278-entry physical-index lookup table in the loaded executable cave.
- Generated `ui_glyph_store_v41_e9ea_probe_patch_only.zip`, SHA256 `D5AC9441CB479F1B0B28B1732C97F52CCD284A8E73BE487AA16E41D9CC37BD78`.
- Static and deterministic audits pass. Runtime verification is pending; v0.39 remains the rollback baseline.

2026-07-18 v0.41 runtime acceptance:
- Confirmed normal rendering of extended Korean skill text and uninterrupted UI selection/progression.
- Confirmed that v0.40 symptoms (colored fragments, blank labels, malformed blocks, and progression stop) no longer occur.
- Promoted v0.41 as the accepted UI glyph-store infrastructure baseline and preserved a frozen copy under `99_backup/baselines/ui_glyph_store_v41_runtime_success_2026-07-18/`.
- Kept inherited v0.39 HUD LV, button-icon, and battle-cursor defects open for targeted repair; no patch binary was changed during this promotion.

2026-07-18 v0.42 guide terminology and v0.39 inherited-defect repairs:
- Added `02_scripts/build_ui_guide_repairs_v42.py` and `02_scripts/audit_ui_guide_repairs_v42.py`.
- Rebuilt from accepted v0.41 and retained its E9/EA parser hooks, sparse glyph assignments, and byte-identical story E2 data.
- Repacked all 503 UI-table records using the v0.26/reference terminology, replacing the abbreviated v0.39 UI wording while preserving the accepted level-up phrases.
- Added 131 sparse glyphs, including separate L and V HUD glyphs, for 409 total lookup entries. The table ends at 0x801A7852 with 14 loaded-cave bytes free.
- Restored confirm/cancel icon pixels from v0.37, moved them to U=180/192, V=228, and added an E7 icon-ID V-coordinate hook at 0x8019C934.
- Restored the battle cursor rectangle from v0.37, removing the v0.38/v0.39 cursor pollution.
- Generated `05_docs/ui_full_v42.csv`, `05_docs/ui_glyph_store_v42_map.csv`, `05_docs/ui_items_equipment_skills_v42_review.csv`, and `05_docs/ui_skill_guide_reference_v42.csv`.
- Generated `03_output/ui_guide_terms_v42_v39_repairs_cumulative_patch_only.zip`, SHA256 `0CEF485BD82D2C7C72EA4F0326D804AC07282773731CD02AF1D6DA83FCFD5EFC`.
- Static, independent, and deterministic audits pass. Runtime verification is pending; frozen v0.41 remains the rollback baseline.

2026-07-18 v0.43 common UI runtime-repair candidate:
- Added `02_scripts/build_ui_runtime_repairs_v43.py`.
- Rebuilt from hash-locked v0.42 while keeping 503/503 audited table records and all 76 non-PSX ZIP members byte-identical.
- Restored 69 common/system pointer payloads erased or orphaned by v0.42 pool repacking.
- Restored the five battle-HUD pointer payloads to the accepted compact v0.39 layout.
- Re-encoded the configuration label at pointer `0x825D8` as `몬스터 도감` to prevent right-column overlap.
- Generated `05_docs/ui_runtime_repairs_v43.csv` and `01_work/analysis/ui_runtime_repairs_v43/` readback/allocation/regression reports.
- Built `03_output/ui_runtime_repairs_v43_cumulative_patch_only.zip`, SHA256 `ACA90EA9529C4D0000D68DC1563F5290DF8C1ABA68AB5E881FBCA103047CAF56`.
- Runtime verification is pending; v0.42 is rejected and frozen v0.41 remains the accepted rollback baseline.

2026-07-19 v0.44 battle HUD LV and E7 icon routing repair:
- Added `02_scripts/build_ui_hud_e7_v44.py`, which requires the exact v0.43 input ZIP and verifies all expected source bytes before editing.
- Replaced the malformed one-cell HUD LV payload with the existing E9/EA `L` and `V` glyph pair and repointed only the unused second HUD pointer to adjacent zero storage.
- Corrected the E7 routing assumption: control bytes `E7 02` and `E7 03` use table indices 1 and 2. Routed those entries to U=192 and U=180 and changed the V-coordinate hook to apply V=228 to those two indices only.
- Kept `COMM.IMG` byte-identical because v0.43 already contains verified clean decision and X pixels at the destination cells.
- Added state-packet evidence under `01_work/analysis/ui_hud_e7_v44_all_states/` and exact diff/build audits under `01_work/analysis/ui_hud_e7_v44/`.
- Built `03_output/ui_hud_e7_v44_cumulative_patch_only.zip`, SHA256 `B54EB0DD526CB0DDE09A68ABB188AD92151BFF3E49767294B0D52E6815EE664A`.

2026-07-22 v0.70 item-name leaf and battle ring-help candidate:
- Added `02_scripts/build_ui_hud_e7_v70_leaf_names_ring_menu.py` and rebuilt from the exact v0.69 patch-only archive.
- Changed the two item-name occurrences for `마력의 잎` and `쓴 잎` from the broken `EA 66` route to v0.69's dedicated manual leaf glyph `E9 75`.
- Replaced the malformed battle help label with `명령 링 열기`. The 23-byte payload plus terminator fits the reserved 32-byte slot with 8 bytes free.
- Kept `COMM.IMG` and all DAT members byte-identical to v0.69; only `PSX.EXE` changed at verified offsets.
- Built `03_output/ui_hud_e7_v70_leaf_names_ring_menu_patch_only.zip`, SHA256 `5E6D34D1B016AEAD61806B654F15E3E25E672945F43838FA3D09E16023D6EAB5`.
- Static readback passed. Runtime verification remains pending, so v0.69 is still the runtime rollback baseline.

2026-07-22 v0.71 font-matched leaf and compact ring-help candidate:
- Added `02_scripts/build_ui_hud_e7_v71_leaf_font_ring_help.py` and rebuilt from the exact v0.70 archive.
- Replaced only the dedicated `E9 75` leaf bitplane at physical index 1356 with the same Gulim 12px, threshold-192, 1-bit renderer used by the established Korean glyph pipeline.
- Removed the awkward `명령` prefix and changed the battle guide to `링 열기`.
- Kept all DAT members byte-identical. Only `COMM.IMG` and `PSX.EXE` differ from v0.70.
- Built `03_output/ui_hud_e7_v71_leaf_font_ring_help_patch_only.zip`, SHA256 `C9FE5D3F3521748CDD53A43A7A47895CC1747928D2804A9502D877B4D47A250D`.
- Static and deterministic readback passed. Runtime verification remains pending.

2026-07-22 v0.72 rejection and v0.73 common-text dual-page renderer:
- Rejected v0.72 after runtime showed Korean glyph fragments overwriting battle-character sprites. The low-page relocation approach must not be reused.
- Added `02_scripts/build_ui_hud_e7_v73_dual_tpage_renderer.py`, based on exact v0.71 SHA256 `C9FE5D3F3521748CDD53A43A7A47895CC1747928D2804A9502D877B4D47A250D`.
- Reserved one glyph packet per common-text state, marked existing row-22+ glyphs in metadata, and rendered common text in low/high texture-page passes.
- Added explicit R3000 load-delay slots throughout the injected renderer.
- Built `03_output/ui_hud_e7_v73_dual_tpage_renderer_patch_only.zip`, SHA256 `70C3CA41D994589DFFD41859C198F0EF371F7A93E71C8196932297A67A4CAA23`.
- Only `PSX.EXE` changes from v0.71. `COMM.IMG`, all DAT files, story data, item names, icons, and existing glyph pixels remain byte-identical.

2026-07-22 v0.73 runtime rejection and v0.74 leaf-RA trampoline fix:
- Analyzed DuckStation slot 1 and confirmed an execution loop inside the common glyph builder: PC `0x8016B60C`, RA `0x8016B5E0`.
- Added `02_scripts/build_ui_hud_e7_v74_leaf_ra_trampoline_fix.py`.
- Replaced the v0.73 glyph-flag `JAL` with a non-linking `J`, and replaced the helper's `JR RA` with a direct `J` to the original continuation.
- Built `03_output/ui_hud_e7_v74_leaf_ra_trampoline_fix_patch_only.zip`, SHA256 `514DFAE1513AB783D2E689AAC2CA710ECDAFBF6E0D8C937C1953199BBB51D6CD`.
- Only `PSX.EXE` changes from v0.73, at five bytes contained in two instruction words. `COMM.IMG` and all DAT members remain byte-identical.

2026-07-28 v83 runtime rejection and v84 startup-clear boundary fix:
- Rejected v83 after DuckStation slot 1 proved that startup had zeroed the P6 glyph and marker helpers at `0x801A86EC` and `0x801A8788`.
- The captured CPU PC/next-PC values were `0x801A8730` and `0x801A8734`, inside the erased helper, confirming the exact reason progression stopped.
- Added `02_scripts/build_ui_hud_e7_v84_p6_sidecar_bss_boundary_fix.py`.
- Moved the startup BSS clear boundary from `0x801A86E8` to the loaded executable end at `0x801A8800`, preserving the complete v83 helper and 12-byte sidecar region.
- Built `03_output/ui_hud_e7_v84_p6_sidecar_bss_boundary_fix_patch_only.zip`, SHA256 `69B696B338A5C2F7727FE3F939CB7EF20D6320F2527227202CD17D4F70C28594`.
- Relative to v83, only `PSX.EXE` changes: two bytes at file offsets `0x5AFC0-0x5AFC1`. The other 77 archive members are byte-identical.
- Static readback passed. Runtime verification must use a cold boot because the v83 slot embeds the erased executable RAM.

2026-07-28 v84 runtime rejection and v85 high-RAM bootstrap candidate:
- Rejected v84 after two DuckStation states showed that the preserved EXE tail is subsequently used as live BSS and overwrites the P6 helper/sidecar.
- Added `02_scripts/build_ui_hud_e7_v85_p6_highram_bootstrap.py`.
- Rebuilt the P6 glyph helper, marker helper, and sidecar for `0x801FE3C4-0x801FE4D7`.
- Replaced the original 56-byte startup clear loop with a same-size bootstrap that copies the 276-byte image to high RAM, then clears the complete original BSS range and resumes the byte-identical original stack setup.
- Kept the PS-X EXE size and load layout fixed. Relative to v84, only 57 `PSX.EXE` bytes change; all 77 other ZIP members are byte-identical.
- Built `03_output/ui_hud_e7_v85_p6_highram_bootstrap_patch_only.zip`, SHA256 `D7D028F83E42EA54922A853250976ADFDE06BE1A3D36DE809E05B4733AC35582`.
- Static audits and a second deterministic build pass. Runtime verification remains pending.

2026-07-28 v86 P6 glyph repack and savestate slots 1-6 patch:
- Added `02_scripts/build_ui_hud_e7_v86_p6_repack_slots_1_to_6.py`.
- Repacked 57 active high-row UI glyph planes into verified blank P6 row-24 planes and narrowed the P6 classifier from rows 24-31 to row 24.
- Added three E2 dialogue replacements identified by slots 1-3 in `31/S3032.DAT` and `32/S3051.DAT`.
- Built `03_output/ui_hud_e7_v86_p6_glyph_repack_slots_1_to_6_patch_only.zip`, SHA256 `41D7A246196D5D985B60C93A84E6D6D11E154F0C0D34EE2AED5BFD11C995678F`.
- Static verification passed. Runtime verification for slots 1-6 remains pending.

2026-07-29 v87 P6 helper heap-reservation repair:
- Added `02_scripts/build_ui_hud_e7_v87_p6_heap_reserved.py`.
- Confirmed from slots 1-4 that `InitHeap` used `0x801FE3C8` as its heap start and overwrote the relocated helper's second instruction.
- Changed the startup heap boundary from `0x801FE3C4` to `0x801FE4D8`, reserving the complete 276-byte P6 helper/sidecar image.
- Built `03_output/ui_hud_e7_v87_p6_heap_reserved_patch_only.zip`, SHA256 `36F578524C94771E88B96C1E90963FDC6AF6C3E1B77ECC4291D5642EBA75DC90`.
- Relative to v86, only two bytes in `PSX.EXE` change. Runtime verification is pending.

2026-07-29 v88 P6 state-persistence and S3032 slots 3-4 candidate:
- Added `02_scripts/build_ui_hud_e7_v88_p6_state_persistence_slots_3_to_6.py`.
- Rejected v87 as a complete symptom fix: it repaired the heap collision, but its single P6 sidecar was still cleared whenever an unrelated non-P6 UI string was parsed.
- Changed the helper so non-P6 strings leave the active P6 pointer and bitmap intact; actual P6 strings still refresh the sidecar, and the existing pointer-equality guards remain active.
- Added the two omitted S3032 dialogue blocks identified by current savestate slots 3 and 4.
- Built `03_output/ui_hud_e7_v88_p6_state_persistence_slots_3_to_6_patch_only.zip`, SHA256 `CB89BF7A66CE1C176D44FD8E49772F4D94A8A4A688C11A95E207F3F89D802FAC`.
- Relative to v87, only `PSX.EXE` and `31/S3032.DAT` change. `COMM.IMG`, `32/S3051.DAT`, and every other archive member remain byte-identical. Runtime verification is pending.

2026-07-29 v89 S3032 story-safe book glyph repair:
- Added `02_scripts/build_ui_hud_e7_v89_story_safe_book_glyph.py`.
- Confirmed that v88 slot 3 stopped at `책` because the story E2 payload contained UI-only virtual code `EA 6F`.
- Reassigned unused story code `E0 2D` from zero-use `듯` to `책` and replaced the single unsafe code in S3032 E2 slot 2.
- Built `03_output/ui_hud_e7_v89_story_safe_book_glyph_patch_only.zip`, SHA256 `4A379BF0AA02D60E89C30E1BD26E9E884DF60AD0F9EF6FBB33537A9B396C9678`.
- Relative to v88, `COMM.IMG` changes by 36 bytes inside one glyph plane and `31/S3032.DAT` changes by exactly two bytes at `0x45124-0x45125`. `PSX.EXE` and every other member are preserved.

2026-07-29 v90 E2 control-boundary and P6 lifetime candidate:
- Added `02_scripts/build_ui_hud_e7_v90_e2_control_and_p6_lifetime_fix.py`.
- Removed two visible `E6 01` pairs from the S3032 E2 secondary string at file offset `0x45100`; this parser treats those bytes as visible glyphs rather than line breaks.
- Updated the high-RAM P6 helper so unrelated text objects preserve pending P6 state, while a new non-P6 string on the same text object clears only the stale active pointer.
- Built `03_output/ui_hud_e7_v90_e2_control_p6_lifetime_fix_patch_only.zip`, SHA256 `496A5F8FB72F342B656D6CDA13819C196D64D27C23BCE35EB511BCECB67C0739`.
- Relative to v89, only `PSX.EXE` and `31/S3032.DAT` change. `COMM.IMG` and every other archive member remain byte-identical.
- Static and deterministic verification passed. Runtime verification is pending, so v90 is not yet the accepted baseline.

2026-07-29 v91 v89 lifetime restore and Yagun slot repair candidate:
- Rejected v90 after runtime testing: selecting the previously malformed item produced a blank name window and froze the game, while the Yagun office line still rendered malformed text.
- Added `02_scripts/build_ui_hud_e7_v91_v89_lifetime_yagun_slot_fix.py`.
- Restored `PSX.EXE` byte-for-byte from v89, removing the v90 same-object/non-P6 sidecar clear that erased a valid pending item-name bitmap.
- Rebuilt only S3032 E2 slot 2 at `0x45100-0x4517F`, preserving its `E6 01` line breaks and replacing the collided `책임질` wording with `그때는 저희도 관여할 수 없습니다.`.
- Built `03_output/ui_hud_e7_v91_v89_lifetime_yagun_slot_fix_patch_only.zip`, SHA256 `60F0D550ECE295DA771440526E7B3B251F113A32DCA25CC8381DB442B9D80027`.
- Relative to v90, only `PSX.EXE` and `31/S3032.DAT` change. Static, deterministic, member-order, and ZIP-integrity verification passed. Runtime verification is pending, so v91 is not yet the accepted baseline.

2026-07-31 v92 stateless P6 pass classifier candidate:
- Added `02_scripts/build_ui_hud_e7_v92_stateless_p6_pass_classifier.py`, which requires the exact v91 input ZIP and verifies eight invariant words plus five source words before editing.
- Diagnosed the recurring malformed high-page glyphs as a data-structure defect: the P6 marker helper keys its bitmap to one global text-state pointer, so glyphs belonging to any second concurrent text object were classified low-page and rendered from the wrong texture page.
- Replaced the helper call at `0x801A2204` with an inline per-packet test `packet[0x29] == 32`, which is exactly equivalent to the helper's `row == 24` classification because the builder stores `V = row * 12` with `sb` and only row 24 truncates to 32.
- Changed the pass-1 tag at `0x801A2140` from `0x80` to `1` so the comparison needs no shift. This keeps `addu a1,v1,s4` at `0x801A2218` as the branch delay slot and leaves the entire loop body at its original addresses.
- Deliberately retained the glyph-builder hook, the 276-byte high-RAM helper image, the entry bootstrap, and the InitHeap reservation. The helper still applies `U += 40` for P6 cells; only its sidecar writes become unread.
- Built `03_output/ui_hud_e7_v92_stateless_p6_pass_classifier_patch_only.zip`, SHA256 `19768F72D6EC84016787909BA982C2D5E3014AD14770B0771BEB7446D9F0040D`.
- Relative to v91, only `PSX.EXE` changes, by exactly 15 bytes in five words. All 78 other archive members are byte-identical.
- Static, deterministic, disassembly-readback, member-order, and ZIP-integrity verification passed. Runtime verification is pending, so v92 is not yet the accepted baseline.

2026-07-31 v93 S3032 speaker-separator repair:
- Added `02_scripts/build_ui_hud_e7_v93_s3032_speaker_separator.py`, which requires the exact v92 input ZIP and asserts the `21/S2021.DAT` reference payload before editing.
- Identified the garbled glyph in the Yagun office dialogue as the `E6 01` code embedded in all four S3032 E2 blocks. The runtime-proven convention in `21/S2021.DAT` at `0x45E00` uses colon `DF 80` plus space `9C` and contains no `E6 01`.
- Replaced the separator after each speaker name with `DF 80 9C` and every remaining separator with `9C`. Block 4 is a speakerless window, so both of its separators became spaces.
- Performed surgical byte replacement only. No character was re-encoded, and each block's trailing byte was preserved exactly.
- Corrected the superseded `codex_notes.txt` claim that `E6 01` is a required line break in these payloads.
- Built `03_output/ui_hud_e7_v93_s3032_speaker_separator_patch_only.zip`, SHA256 `7BCA52B59459F4F71A178D1D0A22454661AD358952C5E11B5EC06B614E038845`.
- Relative to v92, only `31/S3032.DAT` changes, by 100 bytes confined to the four dialogue blocks. Runtime verification is pending.

2026-07-31 v94 P6 rendering-path disable probe (diagnostic):
- Added `02_scripts/build_ui_hud_e7_v94_p6_path_disable_probe.py`, which requires the exact v93 input ZIP and validates every restored word against the v71 archive before writing.
- Located the injected renderer entry: v73 replaced the original prologue of `0x8016B764` with `j 0x801A22A0`. Restoring `0x8016B764`, `0x8016B768` and the glyph-builder hook at `0x8016B5D8` to their exact v71 values is the complete off-switch for the P6 path.
- Established that the P6 hook sits inside the COMMON glyph builder `0x8016B518`, which has five call sites and is shared by dialogue, item names, load/save screens, HUD and E7 icons. This violates the design principle under which the separate storage was approved.
- Built `03_output/ui_hud_e7_v94_p6_path_disable_probe_patch_only.zip`, SHA256 `68B59D5BE63D87DB46678CC92D99A377B14E749ABE0ABB71663C2859B8C5EEF4`.
- Relative to v93 only `PSX.EXE` changes, by 11 bytes in three words. The injected code, high-RAM helper, entry bootstrap and row-24 lookup remain but are unreachable.
- This is a diagnostic, not a release candidate. Row-24 glyphs are expected to render as garbage.

2026-07-31 v94 runtime acceptance and v95 slot-reservation candidate:
- v94 runtime result: PASS. Cold boot showed the corrected Yagun dialogue with the `야군 : ` separator and no freeze on the item name. Row-24 glyphs render as garbage, which was the expected and accepted diagnostic regression.
- Promoted v94 to the accepted rollback baseline and froze a copy under `99_backup/baselines/ui_hud_e7_v94_runtime_success_2026-07-31/`. This also confirms the v93 `E6 01` separator repair, since v94 carries that S3032 data.
- Identified the item-name freeze: the injected driver places its high-page DR_TPAGE at `packet_base + 52*limit + parity*20`, one slot past the packet array. v73/v74 reserved that slot by storing `limit - 1` from a hook at `0x8016B148`; v76 removed the hook and every later build with the renderer writes out of bounds.
- Added `02_scripts/build_ui_hud_e7_v95_tpage_slot_reservation.py`, which requires the exact v93 input ZIP, validates every new word against the v74 archive, checks that the enclosing function saves RA before inserting a `JAL`, and scans the caller for any later read of the clobbered `a0`.
- Built `03_output/ui_hud_e7_v95_tpage_slot_reservation_patch_only.zip`, SHA256 `2646E40C0543EBA95684767D97221FF01573FC9CDE0D4F981749A4B11F54AA8F`.
- Relative to v93 only `PSX.EXE` changes, by 19 bytes in six words. Runtime verification is pending.

2026-07-31 v96 rejection and v97 measured high-page tpage:
- v96 runtime result: FAIL. Item and skill names still rendered as garbage, though the freeze stayed fixed and dialogue remained correct.
- Cause: v96 set the high-page texture page to 6, taken from the `codex_notes` line "absolute texture area x=1576..1755". Those coordinates are relative to the COMM.IMG strip, not absolute VRAM.
- Established the COMM.IMG to VRAM mapping by content matching against a savestate: 512 rows of 896 bytes, left edge at VRAM x4bpp 1280, rows 1:1 with VRAM rows.
- Measured the real P6 destination by diffing v85 against the current COMM.IMG: y 288..298, x4bpp 2856..3033, blank before the v86 repack. Isolating the bitplanes shows clean 12x12 Hangul.
- x4bpp 2856 is in texture page 11 (base 2816), and 2856 - 2816 = 40, matching the glyph helper's `addiu a3,a3,40` exactly.
- Added `02_scripts/build_ui_hud_e7_v97_high_tpage_measured.py`. It measures the P6 extent from COMM.IMG at build time, derives the texture page from it, refuses to build unless the derived U offset equals the helper's +40, and only then writes the instruction.
- Built `03_output/ui_hud_e7_v97_high_tpage_measured_patch_only.zip`, SHA256 `5DCD2E995D1BB4F1100489405DD7350F3492E35A686498A55FEE644742E1D6D4`.
- Relative to v96 only `PSX.EXE` changes, by one byte: `ori a3,a3,0x0016` becomes `ori a3,a3,0x001B`.

2026-07-31 v97 runtime acceptance and v98 capacity restoration:
- v97 runtime result: PASS on every checkpoint. Item name, skill name, the S3032 Yagun dialogue, the load/save screen, menus, battle and the red ARC transition all correct, with no freeze. This is the first build in which the expanded glyph store is actually read and displayed.
- Promoted v97 to the accepted baseline and froze a copy under `99_backup/baselines/ui_hud_e7_v97_runtime_success_2026-07-31/`. v94 is retained as the fallback for shutting the P6 path off.
- Investigated whether the high-page tpage primitive could live inside the text-object struct. It cannot: the structs are exactly 68 bytes apart and the initializer already fills 0..67, including two low-page tpage slots at +44 and +56.
- Added `02_scripts/build_ui_hud_e7_v98_restore_object_capacity.py`. It restores the initializer at `0x8016B148` to byte-identical original game code, clears the injected helper, and moves the reservation into the driver as `base + 52*(limit-1)`.
- Built `03_output/ui_hud_e7_v98_restore_object_capacity_patch_only.zip`, SHA256 `526232330287EEF6AA66A1020B2C0E472DA936CABD90DD55FFA9A31F2ADB36B3`.
- Relative to v97 only `PSX.EXE` changes, by 25 bytes in eight words. Every text object regains its full glyph capacity, and one of the three common-path hooks is gone.

2026-07-31 terminology reference and decision record:
- Read the scanned guidebook in `05_docs/참고사항/공략본/` (GAME MAGAZINE pp.88-96) and extracted its item, accessory, skill, summon, stat, status, character and place tables.
- Added `02_scripts/build_reference_glossary.py`, which holds the extracted terms, cross-checks them against `ui_full_v42.csv` and the glyph store, applies the agreed ruling order and writes `05_docs/terminology_decisions.csv`.
- Result over 134 terms: 29 already agree with the patch, 13 keep the current wording, 4 adopt the guidebook, 88 are guidebook-only terms not yet present in the UI table.
- Established that the guidebook is a reference rather than an authority. It is strong for items, accessories, stats and places, weak for skill names where it transliterates kanji, and demonstrably wrong in four places.
- Recorded that glyph availability must not drive word choice. Every remaining decision costs at most 7 new glyphs against 27 free slots.

2026-07-31 v99 rejection and v100 frame-boundary probe (both diagnostic):
- Added `02_scripts/build_ui_hud_e7_v99_loadimage_probe.py`. It hooked the text-render entry and called `LoadImage` with a small harmless rectangle.
- v99 runtime result: FAIL. Sprites vanished and progress stopped. The savestate showed the CPU inside LoadImage's GPU wait loop (`ra = 0x80177FD0`, `s6 = 0x80177E4C`, `s2 = 0x04000000`): calling it mid-frame deadlocks, because the frame it blocks is what would free the GPU.
- Added `02_scripts/build_ui_hud_e7_v100_loadimage_frameboundary.py`, built from v98 rather than v99 so the failed hook is not carried. It replaces the frame-swap call at `0x8011C4AC` with a stub that runs `LoadImage` first, at the point where the GPU is idle.
- v100 runtime result: PASS. The game runs normally. A small block of corruption appears on the title screen at the transfer destination, which confirms the transfer occurred; later screens do not display that area.
- This resolves the main unknown for plan D-lite: a CPU-to-VRAM transfer is possible once per frame at the frame boundary.
- Built `03_output/ui_hud_e7_v100_loadimage_frameboundary_patch_only.zip`, SHA256 `B0B94915DC89AFA259834A0B5EC32840130108AE1084A4DA02140FCC215AFBAF`. Relative to v98 only `PSX.EXE` changes, by 53 bytes.

2026-07-31 v101 glyph RAM upload (first build rejected, rebuilt):
- Added `02_scripts/build_ui_hud_e7_v101_glyph_ram_upload.py`. It appends the 1080-byte P6 glyph block to `PSX.EXE`, replaces the entry bootstrap with a routine at `0x80193B44` that relocates both the P6 helper and the glyph block before clearing BSS around them, and retargets the v100 stub to upload from `0x801CDE00` to the real P6 rectangle.
- First build rejected. Two defects: the clear loop ended at `0x801FE4D8` and so erased the P6 helper it had just copied, and the build was based on v98, which lacks the v100 stub and hook, so `LoadImage` was never called.
- Both fixed. The clear now ends at `0x801FE3C4`, the base is v100, and the builder refuses to proceed if any relocated block overlaps a clear range or if the clear coverage no longer matches the original span.
- `PSX.EXE` grows from 581,632 to 583,680 bytes, so the ISO must be rebuilt with mkpsxiso.
- Rebuilt output SHA256 `3DC4A81533311800D2A9848479A75255E76F501047C10BCFE4518F0E26A178EC`. Runtime verification pending.

2026-08-01 v101/v102 rejection and the search for a glyph home:
- v101 runtime result: FAIL. The screen advanced but the game stopped progressing, without crashing.
- Added `02_scripts/build_ui_hud_e7_v102_bisect_no_upload.py`, which is v101 with the `jal LoadImage` replaced by a `nop`, to separate the transfer from everything else the build changed.
- v102 runtime result: FAIL, identically. The transfer is not the cause. Two suspects remain: the write to `0x801CDE00`, and the replacement bootstrap with its split BSS clear.
- Re-examined `0x801CDE00`. It is not free: 54 bytes inside it are rewritten periodically, with the same values in both savestates checked. It had been accepted on the grounds that it was zero in every savestate, which is the same reasoning that had already failed once.
- Established the process rule that follows from it: "unchanged across savestates" means the game may only be reading the region, not that it is unused, and the savestates cover about 17 distinct loaded scenes rather than 120 independent ones.
- Searched the loaded executable image instead of BSS. A dead-code scan reported 126.7 KB of uncalled functions with a 25,896-byte contiguous stretch at `0x801A22D8`, of which `0x801A23D8..0x801A73D7` never changed across 133 savestates.
- Rejected it before writing anything. Back-tracing found 84 lui-formed addresses landing inside, and the contents are smoothly increasing 16-bit tables. It is read-only data the game depends on; the scan had misread a data region as uncalled code because it has no function prologue.

2026-08-01 v103 heap-reserved glyph store:
- Changed approach from finding unused memory to reserving memory. The startup calls `InitHeap(0x801FE4DC, size)`; raising that address means the allocator is told the space is not available, which is a contract rather than an observation. v87 already reserves 276 bytes this way and has held.
- Measured the heap across 133 savestates to confirm the game still has room: nothing below `0x801FFA60` is ever written, and no block inside the proposed reservation is ever non-zero. Reserving 1076 bytes at the bottom leaves over 5 KB of untouched heap.
- Found that the existing bootstrap already does the required work. It copies the 276-byte P6 helper from `0x801A86EC` to `0x801FE3C4` and only then clears BSS, `0x801A86EC + 276` is exactly the end of the loaded image, and the copy destination is exactly where the clear stops.
- Therefore appending the glyph block to `PSX.EXE` places it immediately after the helper source, and widening the single memcpy from 276 to 1356 bytes carries both across in one call to a destination the clear cannot reach. The appended source is zeroed afterwards, which is harmless.
- The BSS clear is byte-for-byte identical to v100's, and no replacement bootstrap is needed. Both of v101's surviving suspects are gone.
- Added `02_scripts/build_ui_hud_e7_v103_heap_reserved_glyphs.py`, built on v100. Relative to v100 it changes six words and appends one 2048-byte sector: memcpy length at `0x801757CC`, heap base at `0x80175810`, the stub's source register at `0x801A2084`/`0x801A2090`, and the rectangle at `0x801A22E4`.
- The v100 scratch rectangle is replaced by the real P6 rectangle (x 714, y 288, w 45, h 12), so the corruption seen on the post-BIOS screen should disappear; it was this stub writing the executable's first bytes into a visible part of the framebuffer.
- Verified by disassembling the built archive and walking the startup as the CPU runs it: copy source ends at `0x801A8C38` inside the image, glyphs land at `0x801FE4D8..0x801FE90F`, the new heap starts at `0x801FE910`, and the bytes the stub uploads match the COMM.IMG P6 block.
- Built `03_output/ui_hud_e7_v103_heap_reserved_glyphs_patch_only.zip`, SHA256 `9EE40993E72962F26DAFBD61CA565D4646E247D9990B79EF5122776838584FD3`. `PSX.EXE` is 583,680 bytes, so the ISO must be rebuilt with mkpsxiso. Runtime verification pending.

2026-08-01 v103 runtime result and the actual cause of v101, v102 and v103:
- v103 runtime result: FAIL, breaking and stopping right after BIOS.
- The savestate cleared the patch itself. All four edited startup words were the v103 values, the P6 helper had relocated correctly, the 1080-byte glyph block was byte-identical to the COMM.IMG source at `0x801FE4D8`, and VRAM rows 288..299 columns 714..758 held the atlas and nothing outside them did. The reservation, the copy and the per-frame upload all worked.
- The heap change was also cleared, and turned out to be inert. The original arithmetic computes a negative heap size (`InitHeap(0x801FE4DC, -25816)`), so this game effectively never allocates. Moving the boundary changed nothing either way.
- That left the only remaining difference from v100: the file size. `PSX.EXE` contains no `COMM.IMG` or `COMM.DAT` string and no `cdrom` string, but it does contain their sector numbers -- 307, 667 and 891 -- in a table at `0x80191154`. 500 of the disc's 507 file sector numbers appear in the executable.
- `PSX.EXE` is the first file on the disc, occupying sectors 23..306, and COMM.DAT begins at 307 with no gap. Growing the executable by one sector therefore moves every other file by one.
- Confirmed against the image the failure was captured on: all 506 data files had shifted by exactly one sector, COMM.DAT to 308, COMM.IMG to 668, COMM.SND to 892, while the executable content itself matched the v103 build.
- This is the shared cause of v101, v102 and v103, which are exactly the three builds that changed the file size; v98 and v100 did not and both passed. The replacement bootstrap, the split BSS clear and the write to `0x801CDE00` were never the problem. The v102 bisection was read too narrowly at the time: it ruled out the transfer but left both the destination and the file size standing, and only the destination was pursued.
- Added `02_scripts/verify_iso_layout.py`, which compares a built image against the untouched original and fails if any data file changed sector number or if `PSX.EXE`'s content does not match the intended build. A layout error is otherwise silent until the game reads something.

2026-08-01 v104 disc layout, so the executable can grow:
- Considered and rejected patching the sector numbers: 1073 word-aligned words in the executable equal some file's sector number against only 507 files, they sit in at least two differently strided tables, and seven files after `PSX.EXE` have no sector number in the executable at all. A single miss would surface as a wrong scene load much later.
- Considered and rejected keeping the size and splitting the upload: the largest zero run in the image below the BSS clear is 128 bytes, 327 bytes in total.
- Considered and rejected a VRAM master copy refreshed with a VRAM-to-VRAM blit: no 45x12 area anywhere in the COMM.IMG strip is identical across all 134 savestates, so there is nowhere stable to keep it.
- Considered and rejected overwriting an uncalled function: the six candidates of sufficient size are all real code with real prologues, and "no direct caller" is weaker evidence than it looks.
- Chose to change the disc instead of the executable. Added `02_scripts/build_iso_layout_v104_exe_at_end.py`, which writes `E:\arc\arc1_v104.xml`: a 284-sector dummy holds open the slot `PSX.EXE` used to occupy, so COMM.DAT still starts at 307 and nothing else moves, and `PSX.EXE` is placed at the end of the disc where it may be any size.
- This touches no game code, and its failure mode is immediate and total rather than a subtle late corruption. It also removes the size ceiling permanently, which the remaining glyph rows will need: the full script projects to roughly 947 distinct syllables, about seven more atlas rows.
- `PSX.EXE` itself is unchanged from v103; only the disc layout differs.
- v104 layout verified against the built image: all 506 data files kept their sector number, COMM.DAT/COMM.IMG/COMM.SND are at 307/667/891 as the executable expects, and `PSX.EXE` sits at LBA 268481 with content matching the v103 build. `verify_iso_layout.py` reports PASS.
- The game boots and runs. This is the first build in which plan D-lite is actually live: the glyph block is re-uploaded to the P6 rectangle once per frame from reserved RAM.
- Note for rebuilds: DuckStation names memory cards after the image file, so building to a new filename presents an empty card. The existing card is `memcards/arc1_1.mcd`; keeping the output name stable avoids it.
- v104 runtime result: PASS on every checkpoint. The post-BIOS corruption is gone, expanded glyphs display correctly, and they remain correct across repeated scene changes. Plan D-lite works.
- The last one is the result that matters. Until now the game reused the P6 area of VRAM and item names decayed as play went on; the per-frame upload undoes that before each frame is drawn.
- Promoted to the accepted baseline and frozen under `99_backup/baselines/ui_hud_e7_v104_runtime_success_2026-08-01/`, which holds the patch archive, the build report and `arc1_v104.xml`. A build is now two parts and both are required. v98 remains the fallback, but it needs the original `arc1.xml` layout.
- Verified the renamed working image: `E:\arc\arc1.bin` is the v104 layout and passes `verify_iso_layout.py`, so the existing memory card `arc1_1.mcd` attaches again.
- The executable size ceiling is gone. The full script projects to roughly 947 distinct syllables against 57 in the store today, about seven more atlas rows, and appending them no longer disturbs the disc.

2026-08-01 v105 runtime result and v106 two-strip relocation:
- v105 runtime result: everything correct except the skill range overlay. At low skill level the range is a single square and nothing is wrong; once it grows to a cross the overlay tiles render as glyph pixels.
- That case cannot be fixed by borrowing and returning. The range is shown while the skill name is on screen, so the patch holds the rectangle exactly when the game wants it. The game and the patch want the same VRAM at the same moment.
- Confirmed the conflict from the other side too: across 35 v98 savestates the rectangle held the atlas in 26 and a real game texture in 9, in three distinct forms. v98's decaying item names and v105's broken overlay are the same conflict seen from either side.
- Measured every position a strip could occupy across 139 savestates, requiring no savestate to have ever had a non-zero pixel there, and requiring the strip to fit one texture page (U + 12*columns <= 256). 15 columns: no free position. 14: none. 13: seven.
- A single 13-column strip holds 52 glyphs against 57 in use, so it would cost five. Exactly one pair of 13-column strips shares an x and a page, and a pair holds 104.
- Rejected the cheap two-value classifier. `(V & ~bit) == VA` is exact and fits the four inline instructions, but it forces the two V values one bit apart, which only admits six-column strips. A range test cannot work either: the base rows put a V value every 12, so any range wide enough for two strips swallows one.
- Moved the classifier into a subroutine instead. Safe and general: the loop already contains `jal 0x80178F84`, `ra` is saved at `0x801A21DC` and reloaded at `0x801A2294` after the loop, and `at` and `t0`..`t9` are never touched between `0x801A21DC` and `0x801A229C`. The full script needs roughly 947 glyphs, about eighteen strips, so a subroutine is required eventually regardless.
- Rewrote the relocated helper rather than patching it. Its row test and U offset both change, and its sidecar query at `0x801FE460` has had no caller since v92 made the classifier stateless; the builder refuses to proceed if one appears.
- Added `02_scripts/build_ui_hud_e7_v106_two_strips.py` and `05_docs/v106_two_strip_design.md`.
- Result: tpage `0x1F` (page 15,1), x 961..999, U offset 4; strip A y 480 V=224 row 40, strip B y 500 V=244 row 63. 104 slots, 57 in use, 47 spare. All 57 lookup entries remapped off row 24.
- Verified by disassembling the built archive: the classifier tests V against 224 and 244, the helper tests rows 40 and 63 and adds 4 to U, row 40 column 0 lands at x 961 y 480 and column 12 at x 997 inside the strip, and the repacked pixels round-trip with 633 + 68 = 701 non-zero bytes, matching the original block.
- Built `03_output/ui_hud_e7_v106_two_strips_patch_only.zip`, SHA256 `312DCBE1BD0A0EA4D11CFEBC23E91F69D1AADFCEDD2429AC86D10303B751B523`. `PSX.EXE` grew one sector to 585,728 bytes, which is free now: under the v104 layout it is the last file on the disc. Runtime verification pending.
- Pointed `verify_iso_layout.py` at the v106 archive.

2026-08-01 v106 runtime result and v107:
- v106 runtime result: FAIL on both counts. The expanded glyphs rendered blank, and the skill range overlay still showed letters.
- Diagnosed the overlay first, and it turned out not to be an upload problem at all. The original COMM.IMG is all zeros at 4bpp x 2856..3035, y 288..299, and the glyph store was written into that blank space. The game uploads COMM.IMG itself, so it paints the glyphs onto that rectangle regardless of what the patch does at runtime. That is what the range overlay has been showing all along, and it survived the move because the move only changed where the patch writes.
- Fix: blank those 1080 bytes in COMM.IMG, restoring the file to what it originally held there. The glyphs have not needed to be in COMM.IMG since v103; they travel in the executable and are uploaded from reserved RAM.
- Diagnosed the blank glyphs from the savestate. Everything was correctly in place: the relocated helper, both glyph strips, the shim, the classifier, the frame routine and both rectangles all matched the build byte for byte, and both new VRAM strips were empty. The state words read (0, 0), so the countdown was not armed at that moment and nothing was uploaded.
- Fix: remove the countdown. It existed so v105 could return a rectangle it shared with the game. The new location was chosen because no savestate has ever had a non-zero pixel in it, so there is nothing to share and nothing to return. Uploading unconditionally is what v104 did, and that upload demonstrably worked. This also drops the backup buffers and the state words, and returns the call at `0x801A2198` to the real DR_TPAGE constructor.
- Added `02_scripts/build_ui_hud_e7_v107_unconditional_two_strips.py`. Five words change against v106 and 701 bytes are cleared from COMM.IMG; neither file changes size, so the v104 disc layout still applies.
- Reserved RAM shrinks to helper 276, strip A 936, strip B 936, classifier 36, frame routine 64, leaving 3540 bytes clear of heap the game has been seen to use.
- Built `03_output/ui_hud_e7_v107_unconditional_two_strips_patch_only.zip`, SHA256 `2BE491F12BCF9DC6BA7C4B73D817F2DA981203E92902E6CC10B9D47CF7463982`. Runtime verification pending.

2026-08-01 the battle range overlay, and what v105 to v107 were actually chasing:
- The skill range renders as garbled letters. It grows from one tile to a cross on level-up, and only the four arms use the affected texture, which is why it could not be reproduced before.
- It is not the glyph store. In the frame where it happens no primitive reads the P6 texture page at all, VRAM holds exactly one copy of the P6 atlas, and none of the 800 primitives on screen samples any VRAM the patch modifies. It reproduces identically with the glyphs at (714,288), (714,480), (961,288) and (961,480).
- Built `02_scripts/build_ui_hud_e7_v108_bisect.py`, five builds each changing one thing against v103: the row, the texture page and U offset, the classifier's form, and the two pairs of those. All five render the expanded glyphs correctly and all five show the broken range. That cleared the glyph work entirely and left the two-strip split as the only remaining explanation for v106/v107's blanks.
- The unpatched disc, built with the v104 layout, renders the range correctly. That control settled it.
- In the unpatched savestate the four primitives drawing the cross -- screen (160,64), (128,96), (192,96), (160,128) -- all sample texture page 5,0 at VRAM x 328..336, y 128..160. The original COMM.IMG holds a hollow rectangular frame there, which is the tile the cross is made of. The patched file has Hangul over it: 395 of 594 bytes differ.
- So the Hangul font replacement wrote glyphs onto a UI texture living inside the font atlas. That predates the glyph-store work, which is why moving the P6 strip never helped, and v105, v106 and v107 were all fixing something that was never broken.
- The rectangle covers base-atlas rows 10..13, columns 2..5: 64 slots, of which the lookup table references exactly two, at row 11 column 5 plane 0 and row 11 column 4 plane 3.
- Added `02_scripts/build_ui_hud_e7_v109_restore_range_overlay.py`. It moves those two glyphs into free planes of the P6 strip and restores all sixteen cells to the original pixels. Neither file changes size, so the v104 disc layout still applies.
- The P6 strip is the right home for them: it is blank in the original COMM.IMG, which is the property the base atlas turned out not to have. Every future glyph placement has to be checked against the original file, not against free space in the lookup table.
- The first attempt wrote the relocated glyphs to the wrong address: the P6 row sits at 4bpp x 2856, not beside the base atlas at 1280, and the builder's own readback agreed because it read from the same wrong place. The readback now checks the strip the stub actually uploads. Caught by comparing the built archive against v103 rather than by the builder.
- Built `03_output/ui_hud_e7_v109_restore_range_overlay_patch_only.zip`, SHA256 `244B6739ADE5E4FC01D4763BE797F832B64789BE8C4D55B8A43549A21CF383A2`. Verified independently: the P6 strip gains 46 and 49 pixels in column 14 planes 1 and 2, and the sixteen overlay cells match the original byte for byte. Runtime verification pending.

2026-08-01 v110, and the audit that should have existed from the start:
- The range overlay raised the obvious question: what else did the font replacement overwrite? It is answerable without the emulator. A byte the patch changed is safe only if the original byte was zero, which is empty space the patch may use; a changed byte that was non-zero in the original is destroyed artwork.
- Added `02_scripts/audit_overwritten_game_art.py`. Against v109 it found 5,949 destroyed bytes across 120 glyph cells, of which the range overlay was four. 117 of the cells carry no glyph at all: the artwork was overwritten for nothing.
- That is the same fault as the range overlay, 116 more times over, each waiting for whatever draws it to appear on screen. The range overlay only surfaced because a skill levelled up and its cursor grew from one tile to a cross; the four arms use the damaged tile and the centre does not.
- Added `02_scripts/build_ui_hud_e7_v110_restore_all_game_art.py`. It restores all 120 cells and relocates the three glyphs that sat on them.
- Relocation targets are chosen by the only property that has held up: entirely blank in the ORIGINAL COMM.IMG. Being unreferenced by the lookup table proves nothing, which is exactly how this happened -- the range overlay's cells were unreferenced and the game was drawing them.
- Built `03_output/ui_hud_e7_v110_restore_all_game_art_patch_only.zip`, SHA256 `6C621CACC76F224F3E349C3A8ECAD58D6B068B52EB8A91CF77E440BCFCF28F35`. 120 cells and 6,939 bytes restored, 3 glyphs moved to rows 9 columns 18..20. Neither file changes size.
- Re-audited: 8,114 bytes still differ from the original and every one of them was zero there. No game artwork remains overwritten. Runtime verification pending.
- Standing rule from here on: run the audit before accepting any build that touches COMM.IMG. Glyph placement must be checked against the original file, never against free space in the lookup table.
2026-08-01 - Measured-script corpus attempt (incomplete; no game asset changed)
- Added `02_scripts/measure_full_script_requirements.py`, which reads `E:\arc\원본\arc1.bin` through ISO9660 without writing to it and emits reproducible source/reconciliation/glyph CSVs.
- Generated `script_original_full.csv`, `script_translation_reconciliation.csv`, `script_translated_full.csv`, and `syllable_requirement.csv` under `05_docs`.
- This is not a completed full-script translation or final glyph requirement: 4,376/5,795 extracted records retain undecoded glyph/control markers and 5,140 records have no unambiguous existing translation.

2026-08-01 - Original Japanese font-index anchor table (no translation work)
- Added `02_scripts/build_japanese_font_index_map.py` and generated `05_docs/japanese_font_index_map.csv`.
- The table contains all 1,240 index positions. 560 are anchored by complete source rows; 680 remain explicitly unresolved. No corpus or external-reference mapping was accepted without unambiguous proof.
- Regenerated `script_original_full.csv` using only the reviewed anchor table. Its complete-decode count remains 1,419/5,795; this establishes no unsupported characters.

2026-08-01 - Japanese font-index corpus propagation
- Added 106 `corpus` mappings to `japanese_font_index_map.csv` through repeated or uniquely constrained decoded Japanese contexts; retained 560 anchors and accepted no reference mapping.
- Regenerated `script_original_full.csv` read-only from the original disc. Complete rows rose from 1,419 to 1,843; unresolved indices are 574/1,240.
- The builder preserves corpus provenance on subsequent passes rather than relabelling it as an anchor.

2026-08-01 - Full-script translation start, reviewed dialogue batch 1
- Restored the ASCII font rule to the source corpus, then added 33 manually reviewed ordinary-dialogue translations without changing any existing translation.
- Added `02_scripts/apply_script_translation_batch.py`, which refuses to overwrite existing entries and regenerates the whole-corpus syllable report.
- The full script remains incomplete: 1,604 readable dialogue candidates are still untranslated, 361 rows have unresolved glyphs, and 3,142 rows contain controls or are non-dialogue/parser candidates.
- Corrected the batch's `トッシュ` spelling to the binding decision `토슈` and regenerated the syllable report.

2026-08-01 - Translation corpus integrity repair and reviewed batches 2–3
- Cleared Korean text and translation-source labels from all 301 rows whose `japanese` cell still contains `<G:`; no Korean remains on a marker-bearing row.
- Made `02_scripts/apply_script_translation_batch.py` idempotent: it clears this violation before applying a batch, rejects unresolved source rows, and refuses to change either existing translations or completed new translations.
- Added 34 manually reviewed dialogue translations (four White-family introduction lines and thirty Ramada-monk quiz/reward lines) and regenerated `syllable_requirement.csv`.

2026-08-01 - ASCII glyph-rule scope repair (source data only)
- Read the original disc's COMM.IMG at LBA 667, one 12x12 bitplane per index, and recorded the per-index evidence in `05_docs/ascii_glyph_rule_audit.csv` with the raw bitmaps under `01_work/analysis/ascii_glyph_audit/`.
- The formula `glyph index = ASCII code - 32` is verified only for indices 0..25. Index 0 is the blank ASCII space; indices 26..94 are Japanese/non-ASCII shapes and are explicitly unresolved by this rule.
- Repaired only raw-byte positions where a rejected-range glyph had actually become its ASCII candidate: 277 source rows / 562 glyph occurrences, indices 89 and 90. Each is now `<G:n>`; no Japanese character was guessed.
- Restored 111 Korean cells exactly from the task-start table after the mistaken index-0 clear. Cleared 76 Korean cells whose source row was genuinely restored to `<G:89>` or `<G:90>`. Raw/token alignment failures were zero.
- Updated `apply_ascii_glyph_rule.py` so a future run cannot reapply the rejected 0..94 rule. No game image, output archive, backup, ZIP, or emulator was touched.

2026-08-01 - Reviewed script translation batch 20260801-001
- Added 33 reviewed, source-verified Korean dialogue translations in `7/S7021.DAT` through `7/S7026.DAT` to `05_docs/script_translated_full.csv`.
- Added `02_scripts/apply_reviewed_translation_batch.py`, `01_work/translation_batch_20260801_001.csv`, and `05_docs/translation_batch_log.csv`.
- The application tool is source-keyed, refuses pre-existing Korean text and `<G:` sources, and records both canonical CSV hashes for reproducibility.
- No existing Korean cell or Japanese source cell was edited; no original/game-output/backup/ZIP file or emulator was touched.
2026-08-01 - Reviewed script translation batch 20260801-002
- Added 19 reviewed, source-verified Korean dialogue translations in `D/SD014.DAT`, `D/SD015.DAT`, `D/SD021.DAT`, and `D/SD071.DAT`.
- Added `01_work/translation_batch_20260801_002.csv` and appended its measured hashes to `05_docs/translation_batch_log.csv`.
- No existing Korean cell or Japanese source cell was edited; no original/game-output/backup/ZIP file or emulator was touched.
2026-08-01 - Reviewed script translation batch 20260801-003
- Added 13 reviewed, source-verified Korean dialogue translations in `B/SB021.DAT` and `B/SB022.DAT`.
- Added `01_work/translation_batch_20260801_003.csv` and appended its measured hashes to `05_docs/translation_batch_log.csv`.
- No existing Korean cell or Japanese source cell was edited; no original/game-output/backup/ZIP file or emulator was touched.

2026-08-01 CTRL00 parser-width correction
- Corrected `02_scripts/measure_full_script_requirements.py` so record ends are found only on runtime token boundaries and `E1..FF` controls consume their argument byte.
- Applied the same token-width rule to `02_scripts/extract_story_corpus.py` to prevent the legacy analysis path from recreating split controls.
- Removed the duplicate COMM.DAT target append; COMM.DAT remains scanned once as one of the 256 DAT files.
- Restored 67 manual/exact Japanese atlas mappings erased as a side effect of the earlier broad-ASCII audit; no mapping was inferred and unresolved indices 89/90 remain blank.
- Regenerated `script_original_full.csv`, `script_translated_full.csv`, `script_translation_reconciliation.csv`, and `syllable_requirement.csv`. Rows changed 5,795 -> 2,878; `<CTRL:00>` rows 3,027 -> 0; all 2,024 existing Korean/provenance cells were preserved exactly.
- Added read-only audit/probe/verification tools and generated `ctrl00_parser_audit.md` plus the full 179-file distribution CSV.
- No original disc, `03_output`, `99_backup`, ZIP, or emulator was touched.