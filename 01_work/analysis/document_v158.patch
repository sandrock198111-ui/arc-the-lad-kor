diff --git a/05_docs/changelog.txt b/05_docs/changelog.txt
--- a/05_docs/changelog.txt
+++ b/05_docs/changelog.txt
@@ -1172,0 +1173,12 @@
+
+2026-08-08 - v158 완성형 글리프 동적 캐시, 정적 검증 통과
+- v151 `arc1_v151_free_the_sprite_cell_A4358FEE.zip`을 되돌리기 기준으로 삼았다. 이전 성공 파일은 덮어쓰지 않았다.
+- 매 프레임 고정 위치에 올리던 strip A/B/C/D와 고페이지 2패스 렌더러를 제거하고, 게임의 원래 단일 페이지 렌더러를 복구했다.
+- 현재 대본에서 실제 E2가 가리키는 슬롯 1,431개만 다시 배치했다. 빈 바이트가 있다는 이유로 슬롯으로 오인하던 넓은 스캔은 사용하지 않는다.
+- 완성형 한글 699종 가운데 492종은 원래 글자 페이지에 고정하고, 나머지 207종은 20칸 동적 캐시로 요청될 때 교체한다. 한 대사 단위에서 동시에 필요한 동적 글자는 실측 최대 4종이다.
+- COMM.IMG의 글자 열을 원본으로 복구한 뒤 안전한 고정 한글과 원본과 달랐던 UI 글리프 5개만 다시 배치했다. 캐시는 12x12 완성형 비트맵을 그대로 사용하며 조합형 글꼴이 아니다.
+- 예약 RAM은 기존 5,356바이트, 힙 시작은 0x801FF8B0으로 유지했다. 새로 RAM을 더 빼앗지 않는다.
+- 독립 검증에서 한글 사용 64,866곳, 제어 토큰 4,881개, 보호 UI 5개, 캐시 교체 조합 4,140개를 통과했다. 디스크의 데이터 파일 506개는 원본 LBA를 유지하고 패치 파일 164개가 ZIP과 일치한다.
+- v153~v157은 주소 계산, 조회 범위, 실제 슬롯 판정, UI 글리프 보존 문제를 정적 검사에서 발견해 폐기했다. 실기용으로 사용하지 않는다.
+- 패치 ZIP: `03_output/arc1_v158_dynamic_cache_51FDCF67.zip`, SHA-256 `51FDCF6785E522032F57CB1B043941B6B2F15D575F1FA564BCBE0D92D69ABE75`.
+- 테스트 디스크: `03_output/ARC1_V158_DYNCACHE.cue`. 정적 검증은 PASS, 실기 검증은 PENDING. 되돌리기: v151.
diff --git a/05_docs/test_log.txt b/05_docs/test_log.txt
--- a/05_docs/test_log.txt
+++ b/05_docs/test_log.txt
@@ -1880,0 +1881,12 @@
+
+2026-08-08 | v158 동적 캐시 | 빌더 자체 검사 + 독립 검증기 + ISO 배치 검사 | STATIC PASS, 실기 PENDING.
+- 기준 ZIP v151 SHA-256 A4358FEE5FEA4964C9DD376B630894EB5EC133EFEEDD9E5E85BBC5FDD8CA1A46. 출력 ZIP `arc1_v158_dynamic_cache_51FDCF67.zip`, SHA-256 51FDCF6785E522032F57CB1B043941B6B2F15D575F1FA564BCBE0D92D69ABE75.
+- 실제 E2 참조 슬롯 1,431개만 대상으로 재작성했다. 슬롯과 본문 범위 겹침 0, 중복 슬롯 0, 빈 슬롯 0, 미종결 슬롯 0.
+- 완성형 한글 모양 699종 = 고정 492 + 동적 207. 캐시 20슬롯/5물리칸. 한 텍스트 단위의 동적 모양 최대 4종(`21/S2045.DAT 0x47F94`).
+- 독립 의미 대조: 할당 코드 808개, 한글 출현 64,866곳, 보호 UI 출현 8곳, 제어 토큰 4,881개, 보호 UI 이전 5개 모두 일치.
+- 캐시 비트플레인 시뮬레이션은 동적 원본 207 x 캐시 20 = 4,140조합 전부 통과. 다른 세 비트플레인은 보존됨.
+- 변경된 DAT 159개, 텍스트 바이트 17,835개. 어떤 게임 멤버도 크기가 바뀌지 않았고 PSX.EXE 크기는 v151과 같은 587,776바이트다.
+- `ARC1_V158_DYNCACHE.bin` 검사: 데이터 파일 506개 LBA 원본 유지, COMM.DAT 307, COMM.IMG 667, COMM.SND 891, 패치 멤버 164개 전부 ZIP과 일치. RESULT PASS.
+- v153은 프레임 주소 계산, v154는 조회 상한, v155/v156은 가짜 슬롯과 보호 UI 누락, v157은 보호 UI 이전 2개 누락 때문에 실기 전에 폐기했다.
+- 에뮬레이터는 실행하지 않았다. 콜드 부팅과 메모리카드 진입으로 런타임 확인이 필요하며, 옛 세이브스테이트를 불러오면 옛 RAM이 복원되므로 판정에 쓰지 않는다.
+- 우선 확인: 부팅/불러오기, 문제의 두 슬라임, 전투 커서·스킬 범위, `잎`, 아이템·스킬명, 긴 대사 진행, 전투 진입·종료.
diff --git a/05_docs/codex_notes.txt b/05_docs/codex_notes.txt
--- a/05_docs/codex_notes.txt
+++ b/05_docs/codex_notes.txt
@@ -1288,0 +1289,19 @@
+
+고정 스트립 대신 완성형 글리프 동적 캐시를 쓴다 - 2026-08-08
+- 고정 strip A/B/C/D는 게임 그림과 같은 VRAM을 영구 점유한다. 어떤 상태저장에서 비어 보여도 다른 장면에서 쓸 수 있으므로 안전한 고정 위치를 더 찾는 방식은 중단한다.
+- 0 픽셀을 올리는 것은 업로드 중지가 아니다. 해당 VRAM의 게임 그림을 0으로 덮는다. 스트립의 무죄를 판단하려면 LoadImage 호출 자체를 제거해야 한다.
+- v158은 게임의 원래 단일 페이지 글자 렌더러를 복구하고, 원래 글자 페이지 안의 캐시 20칸만 사용한다. 필요한 완성형 12x12 글자를 요청된 칸으로 바꿔 올린다. 자모 조합형이 아니다.
+- 현재 실제 대본에서 필요한 완성형 한글 모양은 699종이다. 492종은 고정, 207종은 동적이다. 한 텍스트 단위에서 동시에 필요한 동적 모양의 실측 최대는 4종이라 20칸보다 작다.
+- 현재 E2 실제 참조는 1,431개다. 0x80바이트 블록에 0이 있다는 이유만으로 슬롯으로 세면 본문과 숫자표를 슬롯으로 오인한다. `bulk_insertion_manifest.csv`의 과거 E2 1,813개도 현재 v151의 실제 상태를 대신하지 못한다.
+- 실제 슬롯 판정은 현재 본문 시작의 E2 포인터를 따라가 유효한 슬롯 번호만 모은다. 이 집합과 본문 범위가 겹치면 빌드를 거부한다.
+- 조회표 범위는 현재 409항목이다. E9/EA 슬롯이 이 범위를 벗어나면 빈 글리프 0으로 처리해야 하며, 범위 확인 없이 조회하면 뒤의 코드/데이터를 글리프 번호로 읽는다.
+- COMM.IMG를 원본으로 되돌리면 보호해야 할 비한글 UI 가상 글리프가 5개다. 슬롯/원본 색인은 116/1356, 375/4414, 405/5294, 406/5295, 407/1317이다. 다섯 모두 원본 COMM.IMG와 모양이 달라 별도 안전 칸으로 이전해야 한다.
+- 동적 글리프는 행 143종 사전(286바이트)과 글리프별 12개 행 번호(2,484바이트)로 저장한다. 조회표 818바이트를 포함한 코드 전 데이터는 2,938바이트다.
+- v151이 확보한 예약 RAM 5,356바이트 안에서 전체 사용량은 3,840바이트다. 힙 시작 0x801FF8B0을 더 올리지 않으며 새 RAM 영역을 추측해 쓰지 않는다.
+- 프레임 코드에서 배열 주소는 `base + index*2`다. 절대 주소에 절대 주소를 더하는 v153 방식은 금지한다.
+- 캐시 코드 하나를 만들 때 그 비트플레인만 바꾸고 같은 물리 칸의 나머지 세 글자는 보존한다. v158은 207x20=4,140조합을 전부 메모리에서 모의 실행해 확인한다.
+- E2/E5/E6 같은 제어 토큰은 글자 재인코딩과 별도로 비교한다. v158 독립 검증은 제어 토큰 4,881개가 v151과 같음을 확인했다.
+- v158은 정적 검증만 통과했다. 실기 확인 전에는 bible_current의 성공 기준으로 승격하지 않는다.
+
+동적 캐시 빌드의 정적 통과 조건 - 2026-08-08
+- 현재 실제 슬롯만 수정, 모든 멤버 크기 불변, 원래 제어 토큰 불변, 각 한글의 최종 비트맵 불변, 보호 UI 불변, 허용 범위 밖 변경 0, 캐시 모든 원본/슬롯 조합 통과, ISO 데이터 LBA 원본 유지가 모두 동시에 성립해야 한다.
+- 빌더 자체 PASS만으로 배포하지 않는다. 별도 구현의 독립 검증기와 `verify_iso_layout.py`가 함께 PASS한 뒤 실기 테스트로 넘긴다.
