# V372 csv:4 한 건 4줄 조판 시험판

> 2026-09-09 사용자 폐기 결정: 대화 화면마다 줄 길이가 달라져 일관성이 떨어지고 지저분해 보일 수 있어 이 조판 방향을 채택하지 않는다. V372 산출물은 비교 기록으로만 보존하며 이후 수정은 V371에서 분기한다. 아래는 과거 실험 기록이다.

## 현재 결과 — 사용자 승인 후 생성

2026-09-09 사용자 “어 테스트판 만들어봐”에 따라 아래4줄 배치를 적용했다.

    새해가 오면 팔렌시아
    성에서 나를 데리러 와.
    그게 우리 일족의
    규율이야.

- CUE/BIN: `03_output/V372_WORDWRAP_ONE_TEST.cue` 및 같은 이름BIN. 기존 V371 보존.
- ZIP: `03_output/arc1_v372_wordwrap_one_TEST_ONLY.zip`, SHA256 `E8EEAF5774B239DB5B98D9A437C1C734F569D6328DB0263E16D770F17A95917E`.
- BIN SHA256 `59CCD2E7F875B7F61C5BEE758A50EB9D3F1171709BF676ADF7D7A11030B9F594`.
- 실제 변경1DAT/75B, 쓰기계획2개(기존 슬롯128B 범위 및 본문42B 범위). 다른163멤버/EXE/COMM/커서/글꼴/타이틀 동일. 정식CSV 미변경.
- E28A 단독 슬롯은 첫 줄만 유지하고 completion40→2. 본문에는 건너뛰는2B 뒤 E6와 나머지3줄을 둔다. 원래 글자코드/비공백 토큰 순서/종료NUL/후속 이벤트 주소 유지, 새 슬롯 없음.
- 실제 E2 처리를 포함한 격리RAM CPU 시험에서180/228px 모두 기대4행 Y32/48/64/80,38패킷, 원래 종료포인터복귀 확인. 해당 S1011 장면의 실제창/초상화/GPU를 관측한 것은 아니다.
- 동일 입력 ZIP 재생성 exact. 원본 트리에서 새 디스크 생성,506데이터LBA/164패치멤버재읽기/507extent동일. 변경29섹터의 양쪽 EDC/ECC58검사 PASS, 미설명payload변경0.
- 실행: 새 CUE로 콜드부팅 → 새 게임 초반 해당 대사 확인. 구 상태저장은 이전RAM을 복원할 수 있어 패치반영 확인에 사용하지 않는다. 메모리카드/상태저장 파일은 수정하지 않았다.
- 재현: `python -X utf8 02_scripts/build_arc1_v372_wordwrap_one.py`, 이어 `python -X utf8 02_scripts/package_arc1_v372_wordwrap_one.py`. 패키징은 기존BIN/CUE 덮어쓰기를 거부한다. 원본부터의 기존고정ZIP계보와 CPU 캡처 의존성은 상속하며, 원본 하나만의 완전한 역사적 재생성을 주장하지 않는다.

실제 화면/다음 대사 진행은 사용자 확인 PENDING인 로컬 TEST_ONLY다. 이번 한 건 이외의 말투/전수 조판 후보는 반영하지 않았다. bible_current는 기존 사용자 성공점 유지, 실제 변경은 changelog/test_log/codex_notes에 기록했다.

## 이전 3줄 후보 실패 기록

2026-09-09 사용자는 V371의 csv:4 / 1/S1011.DAT 한 건의 어절 조판 테스트를 요청했다. 기존 V371을 보존하고 해당 본문/슬롯만 수정하는 초안을 메모리에서 생성했다. 새 ZIP/BIN/CUE는 생성하지 않았다.

## 확인한 범위

- 본문4799E..479C7, 종료NUL479C8. E28A 단독 호출, Bank-A9 슬롯45480..454FF. 기존 completion40.
- 초안은 기존 글자코드(이의DD01 alias 포함)를 보존해 첫 줄만 슬롯에 두고 나머지 두 줄은 inline으로 배치한다. completion3, 원래 본문42B 및 종료 위치 동일. E6는 확장 슬롯 안에 넣지 않는다.
- 기존2878 CSV 행의 확장 해독 전후 차이는4번만, 다른DAT/EXE/COMM 불변인 복사본 초안이다.
- V371 상태의 확인된 Bank-A 주소114000에 해당 슬롯만 교체한 격리 RAM에서 실제 E2 처리/복귀를 실행했다. 본문은 분석용70000에 놓았으며 실제 S1011 장면 진입이나 창 폭을 관측한 것은 아니다.
- 180px CPU 출력은 첫 줄/둘째 줄은 제안대로지만 마지막 줄의 마침표가Y80으로 내려간다. 종료 포인터/글자 수는 정상이다. 3줄 기대값 assertion 실패로 최종 산출물을 만들지 않았다.

## 앞선 설명 정정

`v371_wordwrap_trial/trial.json` csv:4의180px after.rows도 실제로는4줄이며 마지막 줄은 마침표 하나다. 따라서 미리보기와 실제 슬롯 경로가 다르다는 초기 설명은 틀렸고, 둘은 이 관측에서 일치한다. 사용자가 붙여넣은3줄을 그대로 받아들여 “총3줄 유지”라고 설명한 것이 잘못이다.

기존 자동 검사는 alphanumeric 사이의 어절 끊김만 잡고 문장부호의 행두 고립을 잡지 않았으며4행까지 허용해 layout_candidate로 분류했다. 이는 의미상 좋은 조판/사용자 승인3줄을 보장하지 않는다. 후속 도구에는 문장부호 고립을 별도 예외로 검출해야 한다. 현재 광역 도구를 이 요청에 섞어 수정하지 않았다.

## 이후 승인

마지막 두 줄을 `그게 우리 일족의` / `규율이야.`로 나누는4줄 배치를 사용자가 승인했다. 결과는 위 현재 결과 절에 기록했다. 실제 초반 장면의 창/초상화/게임 화면 확인은 남아 있다.

초기의3줄 실패는 기록으로 보존하며 현재 스크립트는 승인4줄 기대값으로 검증한다. 공개 배포 적격/전체 게임 회귀 통과로 승격하지 않는다.
