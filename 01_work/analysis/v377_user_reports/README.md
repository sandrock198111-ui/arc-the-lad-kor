# V377 / v0.9.3 — 69D03814 상태저장 제보 후속

2026-09-11 사용자 제보 1,5,6,7,8을 반영했다. 상태저장 8개 모두 V376_HOTFIX이며 원본 입력 해시는 states.json에 보존했다. 2/4는 별도 수정 지시가 없는 비교 화면으로 확인했다. 원본·기존 빌드·사용자 세이브는 수정하지 않았다.

## 수정

| 제보 | 변경 |
|---|---|
| 1/동일 증상의 3 | `적이 거의 반드시` → `적이 거의 확실히`; 두 번째 줄은 유지 |
| 5 | `레벨업 시 최대 체력` / `증가량 상승`으로 명시 개행 |
| 6 | 스킬 선택 안내의 E7 01 기호 제거. 같은 기호를 사용하는 빈 곳 선택 안내도 함께 수정 |
| 7 | 물의 정령 대사 `자기들이` → `본인들이` |
| 8 | 물의 정령 대사 `후진` → `후우진`; 기존 소환수명과 일치 |

게임 변경은 PSX.EXE와 8/S8061.DAT 두 멤버, 실제133바이트다. 소유자가 확인된 기존 네 UI 문자열 공간만 재사용하고 다른673개 목록의 비대상 문자열을 보존했다. 글꼴/기계어/장비 효과·소유자/스킬 동작은 변경하지 않았다.

정식 script_translated_full.csv의2275/2279 두 한국어 셀도 수정했다. 원본 전체 바이트 백업 canonical_before.csv 및 전후해시 canonical_edit.json을 보존하며 원문·다른 열·나머지2876행은 같다. 빌더는 정식 원고와 두 적용문 일치, 전2878대사 및673UI 포인터의 후진 잔존 여부를 검사한다. 역사적 조사/기존 빌드 문서의 후진 기록은 증거이므로 일괄 치환하지 않았다.

## 원인

1번은 V376에서 부자연스러운 표현을 승인한 검토 오류다. 5번은 자동 개행에 맡겨 증가량까지 첫 줄에 들어가고 상승만 둘째 줄에 남았다.

6번은 V376에서 원본 E7 01을 복원하면서 실제 표시를 검증하지 않은 회귀다. 사용자 RAM의 첫 패킷은 CLUT7AC1/u98/v130/12×16의 비한글 기호이며 캡처에서 깨진 그림으로 관측된다. 동일 RAM에서 실제8016C794 출력기를 호출하여 기존 전체 패킷/끝포인터를 재현했고, 접두사 제거 뒤에는 정상 한글 패킷만 남는다. 깨진 기호가 표시되는 경로를 없앴으며 해당 원본 그림 자산 자체를 복원했다는 주장은 하지 않는다.

6번 문구가 다른 것은 원본에 있던 상황별 도움말 선택이다. 캡처 선택값801F128F..1290은03/04로 스킬 선택/돌아가기 안내를 선택한다. 원본과 V376의 도움말 상태 표7EA04..7EA23은 동일하다. 실제 선택 함수8015F4E8→8016C6D0→8016C720과 출력표82348+4×3이 연결된다. 원문도能力を選んで下さい이며 스킬별 고유 설명을 잘못 출력한 문자열은 아니다. 사용자 입력 전이/해당 기술의 전체 동작까지 새로 검증한 것은 아니다.

8번은 이전 검사가 UI와 대사 간 용어 일치를 놓친 것이다. UI 후우진은 그대로 두고 사용자 승인 표기를 대사·정식 원고에도 적용했다. 본/인/들/이/후/우/진은 모두 기존 글리프로 인코딩 가능하고 비어 있지 않은 실제 폰트 plane을 확인했다. 해당 대사 CPU 출력 패킷도 기대 문자와 일치한다.

## 검증

- UI 상태1/3/5/6 및 대사 상태7의 수정 전 XY·글리프·끝포인터를 캡처와 정확히 재현했다. 수정 후1/3은 두 줄 유지,5는 증가량이 둘째 줄 시작,6은 깨진 기호0개.
- 대사2275는 본인들이 포함2줄,2279는 후우진 포함3줄/원래 끝포인터로 복귀.2279는 상태7의 실제 적재 스크립트·렌더러 전역값을 사용한 격리 CPU 검사이며 상태8의 대사 장면 재현은 아니다. 상태8은 소환수명 후우진을 확인하는 화면이다.
- V376 물의 신전/배우 상태/설명154행346조건/커서1152조건/기존 카드·선택지 회귀 통과.
- 결정적 재빌드 ZIP 동일. 새 디스크 패치164멤버 readback,506개 데이터 LBA/모든 extent 유지. V376 대비32섹터 양쪽 EDC/ECC 검사64회, 미설명 payload 변경0.
- Rev1 원본에서 xdelta 적용 후 목표 BIN 해시 완전 일치, 재인코딩 xdelta byte exact. 패치 ZIP 안에는 xdelta/CUE/README/verification만 포함.

BIN SHA256: F03CB94D395CB9E25502DB494644F0359F6BAAFD8EC578EB795FCA570939697E

ZIP SHA256: A4A0B128BCF4FD5B11F34D2BF434C1FD01A518B3FABD978EDE8D8C9C3A66AD71

## 산출물과 남은 검사

- `03_output/arc1_kor_v0.9.3_rev1.zip`: 일본판 Rev1 원본용 패치
- `03_output/V377_USER_REPORTS.cue` / `.bin`: 로컬 검수용 디스크
- `02_scripts/build_arc1_v377_user_reports.py`, `verify_arc1_v377_reports.py`, `package_arc1_v377_xdelta.py`
- `build_report.json`, `capture_cpu.json`, `package.json`, `xdelta.json`: 적용·CPU·디스크·패치 증거

실제 에뮬레이터에서 V377 새 부팅→메모리카드 로드→동일 화면 확인은 남아 있다. 이전 퀴즈 말투29행/미확정UI6행/DAT94행 및 전체 플레이 검수도 해결 완료로 세지 않는다. bible_current의 실화면 성공 기준은 유지했다. 다음 검수는 새 부팅에서 설명 줄바꿈, 두 도움말 진입, 물의 정령 대사와 후우진 소환 확인이다.
