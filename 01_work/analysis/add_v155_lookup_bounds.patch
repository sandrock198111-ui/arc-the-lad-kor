diff --git a/02_scripts/build_arc1_v154_dynamic_cache.py b/02_scripts/build_arc1_v154_dynamic_cache.py
--- a/02_scripts/build_arc1_v154_dynamic_cache.py
+++ b/02_scripts/build_arc1_v154_dynamic_cache.py
@@ -1,3 +1,3 @@
-"""v154: replace four fixed VRAM strips with a 20-slot on-demand glyph cache.
+"""v155: replace four fixed VRAM strips with a 20-slot on-demand glyph cache.
 
 The cache uses exact completed 12x12 bitmaps.  It is not a compositional font.  Every
@@ -47,5 +47,5 @@
 
 OUT_DIR = ROOT / "03_output"
-OUT_STEM = "arc1_v154_dynamic_cache"
-ANALYSIS = ROOT / "01_work/analysis/arc1_v154_dynamic_cache"
+OUT_STEM = "arc1_v155_dynamic_cache"
+ANALYSIS = ROOT / "01_work/analysis/arc1_v155_dynamic_cache"
 REPORT = ANALYSIS / "build_report.txt"
@@ -125,5 +125,8 @@
     asm.emit(r_type(T2, T0, T2, 0, 0x23))                # * 254
     asm.emit(r_type(T2, T1, T2, 0, 0x21))
     asm.emit(i_type(0x09, T2, T2, -1))                   # virtual slot
+    asm.emit(i_type(0x0B, T2, T9, LOOKUP_N))
+    asm.branch(0x04, T9, ZERO, "invalid_lookup")
+    asm.emit(NOP)
     asm.emit(r_type(ZERO, T2, T3, 1, 0x00))
     load_address(asm, T4, LOOKUP_RAM)
@@ -179,5 +182,9 @@
     asm.branch(0x04, ZERO, ZERO, "finish")
     asm.emit(NOP)
 
+    asm.label("invalid_lookup")
+    asm.emit(move(V1, ZERO))                             # blank glyph
+    asm.branch(0x04, ZERO, ZERO, "finish")
+    asm.emit(NOP)
     asm.label("static")
     asm.emit(move(V1, T4))
@@ -658,5 +665,5 @@
     DISASSEMBLY.write_text("\n".join(disasm) + "\n", encoding="utf-8")
     lines = [
-        "v154 on-demand 20-slot completed-glyph cache",
+        "v155 on-demand 20-slot completed-glyph cache",
         "",
         f"base    {BASE_ZIP.name}",
